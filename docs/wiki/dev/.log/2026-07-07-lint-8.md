# 2026-07-07 — lint 8 (end-of-day; first pass under the widened sweep)

Four checks under the lint-7-widened scope (workspace-wide lock grep, KV-namespace-vs-writers,
companion test paths). Three findings — all fixed.

**Doc-vs-code:** clean across the board — the extended lock-order table verified complete by a
proper name-diff (44 field sites + 2 aliases, rows 1–30 all present; the shell one-liner in the
first attempt produced garbage, the python diff is the shape to reuse); no new KV prefixes since
lint 7 (`model_deploy` writes only tabled ones); sub-handle count = 8 exactly as the front door
claims; feature flags, constants unchanged since lint 7.

**Staleness:**

- **F1 (pre-existing, surprisingly old):** [examples.md](../examples.md) claimed **eleven** coop
  demos / "`ci_smoke.sh` runs all eleven" — the smoke has run **twelve** since `diagnostics`
  shipped with Legible Emergence Phase 5 (2026-07-03); four lints passed over it because none
  counted. Fixed: twelve CI demos named (incl. diagnostics), the honest-rework glosses on
  `mcp_toolgrowth`/`catalog`, and the new **manual** `model_deploy` listed as
  deliberately-not-in-smoke.
- **F2 (expected):** the post-lint-7 session tail was uningested. Fixed:
  [history](../history.md) artifact-library bullet gained the session tail (probe health pass —
  `Installed::probe` had been exposed-but-unconsumed until the coverage review; four
  lifecycle/concurrency tests; the `model_deploy` real-GGUF proof; the closed-enum
  `ArtifactKind` note), and [companions](../companions/companions.md) wasm-host bullet gained
  the probe-consumed clause + demo pointer.

**Orphans & dead links:** all wiki + front-door + coop-README links resolve; folder-notes
present.

**Coverage:** no homeless knowledge beyond F2 — the testing lore from the blob lifecycle test
(ad tombstones land async → poll rounds structurally) is an instance of testing.md's existing
structural-polls rule, not a new entry.

Pages touched: `examples.md`, `history.md`, `companions/companions.md`. Nothing needing a user
decision.
