## [2026-07-02] lint | fourth pass — WS3 confidentiality claim corrected

Triggered by adding the WS3 example (§4.3.3). Grounding WS3 caught a THIRD imprecise
security shorthand (after served-path and sub-group): DataAtRestCipher
(mycelium-core/src/persistence.rs) is **on-disk only** ("data in memory is not [protected]"),
one cluster-uniform cipher per node — NOT per-page, NOT per-clearance, and does NOT give a
cross-node read boundary (every node holds plaintext in memory). The real in-cluster
"only cleared nodes can read" mechanism is application/envelope **payload** encryption (the
section value is ciphertext before the KV write).

**Check 1 (doc-vs-code) — 1 finding, fixed:** runtime-invariants.md said "encrypted (WS3;
ciphertext still floods, only key-holders read)" — false for WS3 (it's on-disk-only,
plaintext in memory). Corrected: split payload encryption (cross-node read boundary) from
WS3 DataAtRestCipher (disk-theft defence-in-depth), with the persistence.rs citation. Design
record §4.3.1 table row + new §4.3.3 carry the full worked example incl. the
cleared-curator-must-decrypt-to-reconcile build constraint.

Checks 2/3/4 clean (numbering 4.3.1–4.3.3 intact; links resolve; the invariant now has its
wiki home from lint-3, refined here).
