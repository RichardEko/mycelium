# 2026-07-14 — wiki-lint (post visual-showcase examples)

**One finding (§4 coverage), fixed.** Scoped to the delta since the 2026-07-13 lints. No product-code
change since; the delta is **examples + tooling**: four new browser-visualised showcase examples
(`microgrid_viz`, `stigmergy_viz`, `redistribution_viz`, `llm_council_viz`), the conway bind/URL fixes,
and the local scale-nightly runner.

## §1 Doc-vs-code
Clean for the delta:
- **Lock-order table** — the new work is all in `examples/` (out of the table's `src/`·`mycelium-*/src/`
  scope); product-lock count unchanged at 70. The `*_viz` `Arc<Mutex<VizState>>` are example code, not
  declared rows.
- **No stale conway reference** — grepped the wiki for `52000`/`5225x`/conway-port/conway-swim; the
  conway port move (52000→40000) and SWIM-disable are not cited anywhere in `docs/wiki/`.
- `ci_smoke.sh` unchanged (the two coop `*_viz` bins are **not** in the smoke), so the pinned "twelve
  CI demos" count in `examples.md` is still accurate.

## §4 Coverage — the finding
`dev/examples.md` enumerated the coop suite, AFN, a2a, and integration suites, but had **no
visual-showcase category at all** — it omitted the long-standing `conway`/`conway-gpu` *and* this
session's four `*_viz`. *Fixed:* added a **Visual showcases** bullet listing all six with their run
commands + ports (`:8090`–`:8094`), noting they run continuously (not in CI smoke), that the `*_viz`
are visual variants of the batch demos, and the pacing lesson (a showcase must be *watchable* — e.g.
the `THINK` dwell for the instant `EchoBackend`).

## §2 / §3
No staleness (delta is examples/tooling, freshly ingested here). The new bullet uses inline-code paths
(no markdown links) → no dead-link risk; the six run targets were verified to exist.

## Calibration
Logged a sharpening (`lint-calibration.md`, 2026-07-14): the examples-page checks had audited the
*coop demo count* but never verified **category completeness**, so the whole visual-showcase category
slipped for many passes. §4/§1 now audits enumeration pages by category (starter · coop · AFN · a2a ·
integration · visual-showcases), not just by pinned count. Folded into the skill's §4.
