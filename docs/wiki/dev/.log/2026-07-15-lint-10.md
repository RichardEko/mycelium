# 2026-07-15 — wiki-lint (10th pass; post artifact-library showcases)

**Clean — no findings.** Diff since lint-9: two new browser showcases (`provisioning_viz` :8097,
`catalog_viz` :8098) + the doc-coverage run-9 wasm run-command fixes. No `docs/wiki/` page claim moved.

## §4 UI-example contract — both new showcases classify COMPLIANT
The tree-derived enumeration (`grep -rl 'include_str!.*\.html' examples`) now surfaces `provisioning_viz`
+ `catalog_viz`; both advertise `ui/viz`, inject `__OPS_CONSOLE_LINK__` **and** `__CONCEPTS__`, carry the
concepts panel, and their documented run command carries `wasm,metrics` → compliant. **Verified live**
(page served with both placeholders replaced; `catalog_viz`'s Ops-Console back-link correctly targets the
*installer*, which outlives the librarian that dies in the demo). Every other `include_str!` hit
classifies (`conway-gpu`/`ops_console` = documented exceptions). No unclassified hit.

## §3 links — clean
The matrix + showcase-table + coop/README rows for the two viz resolve (→ `coop/README.md`, which gained
a **Browser showcases** section linking `ui-example-contract.md` + guide ch.14). `examples.md`'s
enumeration updated to **four coop `*_viz`** / six `*_viz` total.

## §1 / §2 — not triggered / clean
No code-cited wiki claim moved; the wasm run-command corrections are recorded under doc-coverage run 9.

No calibration entry — nothing a prior lint declared clean was found drifted.
