# 2026-07-10 — lint 8 (post doc-coverage-audit remediation)

Delta since lint 7 (6afac9f): the consensus/lock ops pass (metric family + runbooks),
guide/17-federation, the Tier-2 doc close (incl. the GossipError enum rewrite + companions
runbook), Tier-3 polish, and the persisted doc-coverage audit.

## §1 Doc-vs-code — clean
- **Lock-order table:** no new locks since lint 7 (this session's code change — the
  `mycelium_consensus_*` family — is metric *emission* only, no `Mutex`/`RwLock` fields).
  65 field sites + the 2 known type aliases (`GossipRxs`, `SenderLog`). Table current.
- **Cited constants:** `WIRE_VERSION = 12` / `PREV = 11` (matches `wiki.md`, `history.md`);
  `MAX_KV_WRITE_BYTES = MAX_FRAME_BYTES − 64 KiB`; `EXPLAIN_MAX_FANOUT = 32` (matches the
  `dev/diagnostics.md` claim). All confirmed against `framing.rs` / `emergent.rs`.
- **Named gates** cited in `dev/diagnostics.md` all exist (`test_fleet_snapshot_agrees_across_
  three_…`, `test_gateway_fleet_snapshot_endpoint_scope_gated`, `test_explain_fanout_…`,
  `select_explain_targets_caps_the_fanout_…`).
- **New metric names** in `dev/operations.md` (`mycelium_consensus_timeouts_total` /
  `_commit_conflicts` / `mycelium_schema_mismatch`) match the code emissions exactly.
- **KV-namespace / front-door:** `building-on-mycelium.md` reserved-prefix list matches the
  `src/lib.rs` namespace table exactly (22/22). No new top-level prefix this session.

## §2 Staleness — clean
This session's durable knowledge was ingested as it merged (consensus metric family →
`operations.md` + `.log`; companions runbook → folder-note + `.log`; Tier-2 `.log`). The
wiki's `dev/diagnostics.md` already documented `explain` as the gateway-only cross-node
`sys.explain` fan-out — consistent with the Tier-3 intent note added this session (no drift).
The one `00–16` chapter-count hit is in a historical `.log` (2026-07-10-lint-6) — dated
snapshot, not rewritten.

## §3 Orphans & dead links — clean
No dead relative links across `docs/wiki/`; front-door (`faq.md`, `building-on-mycelium.md`)
links all resolve.

## §4 Coverage — 1 finding, fixed
- The persisted **doc-coverage audit** (`docs/analysis/doc-coverage-2026-07-10.md`) had no wiki
  home, while its sibling `ratings.md` is listed in `wiki.md`'s external-artifacts table. Added
  a row.

Pages touched: `wiki.md`.
