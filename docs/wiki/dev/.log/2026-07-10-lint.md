# 2026-07-10 — lint

## §1 Doc-vs-code
- **Lock-order table: clean.** Full-workspace sweep (fields + type aliases) reconciled against
  rows 1–30 name-by-name; all extras were usages/locals/test-scope (`ENV_TEST_LOCK`,
  `ProgressLog`, `spawn_test_server` — all inside `mod tests`). Today's `pinned_peers` is
  papaya (lock-free) — correctly rowless.
- **Named gates: clean** (one false positive: `manual_is_multiple_of` is a clippy lint name,
  not a test). New gates (`secondary_startup_lag_is_not_evaporation`,
  `never_seen_primary_promotes_after_orphan_grace`,
  `succession_chain_late_secondary_joins_promoted_primary`) all exist.
- **Constants: clean.** `WIRE_VERSION = 12` / `PREV = 11`; `MAX_KV_WRITE_BYTES` present;
  `swim_failure_detector: true` default.
- **FINDING (fixed): front-door reserved-prefix list** (`docs/guide/building-on-mycelium.md`)
  was missing `ckpt/` + `ckptw/` (LangGraph checkpointer rows in the `src/lib.rs` table).
  Same drift class as the 2026-07-07 nine-prefix finding — the list must be diffed against
  the lib.rs table every lint.
- **Endpoints: clean.** 8/8 sampled `operations.md` endpoints exist in `src/agent/http.rs`.

## §2 Staleness
- **FINDING (fixed): `scale-tests.md`** had no CI status while the scale suites gained a
  self-hosted nightly today (`scale-nightly.yml`, #157) — added the CI-status paragraph
  (nightly 06:00 UTC, `mycelium-scale` label, deliberately off the PR path).

## §3 Orphans & dead links
Clean — all wiki + front-door relative links resolve; every folder has its folder-note; the
new page is reachable via the testing folder-note.

## §4 Coverage
- **FINDING (fixed): the Docker cluster suites + their CI gate had no wiki page** — the
  week's biggest durable knowledge (five defects found by gating; the self-diagnosing
  harness: ERR trap, node-log dump, Phase-0 barrier, S13 instrumentation; the no-retries
  posture; the overlay-vs-integration S13 naming trap) lived only in `.log` entries. Filed
  **`dev/testing/cluster-suites.md`**; linked from `testing.md` folder-note + `dev.md`
  routing line.

Pages touched: `dev/testing/cluster-suites.md` (new), `dev/testing/testing.md`,
`dev/testing/scale-tests.md`, `dev/dev.md`, `docs/guide/building-on-mycelium.md`.
