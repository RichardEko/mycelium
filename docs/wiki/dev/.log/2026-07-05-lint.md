# 2026-07-05 — lint pass (post curator step-down + external front-door docs)

Scope: commits since [lint-7](2026-07-04-lint-7.md) — `5aa4f72` (building-on-mycelium on-ramp),
`57eed00`/`54efcdc` (README front-doors + corpus DOIs), `0c54910` (wiki-lint scope extension),
`9e42453` (curator step-down, PR #127), `b872a5e` (schemas() typo). Triggered by the user to confirm
nothing else drifted after the `schema()`→`schemas()` fix. **1 coverage fix.**

**1 · doc-vs-code** — clean.
- **External front-door docs (new check):** `building-on-mycelium.md` reserved-prefix list matches the
  `src/lib.rs` namespace table **exactly** (`agent cap cap-group consensus gcap grp mailbox req schemas
  sys tools tuple`); `WIRE_VERSION` "currently 12" = code 12; feature flags all real; sub-handles now
  read `schemas()` (the `b872a5e` fix — verified 0 remaining singular). This is the check that would
  have caught the typo; confirmed nothing else in the doc drifted.
- **Lock table carries.** No `Mutex`/`RwLock` field landed in `src/` or `mycelium-core/src/` this
  interval (the only `src/` change, `5aa4f72`, was the crate-doc link comment). PR #127's new
  `curator_tasks: Mutex<…>` is in the companion `mycelium-wiki` — outside the lock-order table's
  `src/`+`mycelium-core/src/` scope, so not a table row.
- `WIRE_VERSION`=12 unchanged.

**2 · staleness / 4 · coverage** — 1 fix.
- `dev/companions/wiki.md`'s "Curator election + failover" section predated PR #127: it described the
  initial election + ring-failover (incl. the *failover-side* split-brain guard) but not the new
  **election-side** reconciliation. Added the **step-down / sentinel** paragraph — the fixed settle
  window can dual-elect under load; a sentinel applies lowest-id-wins continuously so a higher-id
  curator resigns (stops just its `curator_tasks` loops, retracts the ad, returns to the reader watch);
  deterministic + self-healing; canary `dual_curators_reconcile_to_a_single_writer`.

**3 · orphans & dead links** — clean. All relative links in `faq.md`, `building-on-mycelium.md`, and
the edited `companions/wiki.md` resolve.

Note for the next `/mycelium-analysis`: the split-brain (no curator step-down) was a latent
concurrency defect in the shipped wiki companion — a candidate calibration-ledger entry if Concurrency
Correctness / Robustness scored ≥8 while it existed. Out of wiki-lint scope to add; flagged here.
