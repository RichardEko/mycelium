# 2026-07-07 — lint 7 (post-artifact-library session)

Full four-check pass, run the same day the artifact-library workstream shipped. Six findings —
five fixed, one flagged for decision.

**Doc-vs-code:**

- **F1 (self-caught):** `PrefetchingSource::cache` (`http_source.rs:45`, added earlier today)
  was missing from the lock-order table → **row 23 added**.
- **F2 (flagged, decision needed):** the lock-order table's "all sites in the codebase" claim
  has never covered the data-plane companions — `mycelium-tuple-space` / `mycelium-blackboard` /
  `mycelium-wiki` hold ~20 unlisted `Mutex` sites (store/WAL inners, role regs, task lists).
  Invisible to prior lints because the sweep greps `src/ mycelium-core/src/` only; incoherent
  now that wasm-host rows 20–23 are in. **Scope note added to the table** documenting actual
  coverage (core + mycelium + wasm-host) and the open question; full-workspace inventory NOT
  taken unilaterally.
- **F3:** `dev/operations.md` governance line said `POST /gateway/govern/{tuning,membership}` —
  code (`src/agent/http.rs`) has **three** routes; `timing` added.
- **F4 (the big one):** the `src/lib.rs` KV-namespace-ownership table — the canon CLAUDE.md
  points at — was missing **nine live prefixes**: `svc/` (persistent capability ads), `log/` +
  `clog/` (KV append log + consumer positions), `lock/` (distributed lock), `prompts/` (LLM
  templates), `skills/` (SkillRunner), `installable/` + `comp/` (wasm-host catalogue + confined
  component KV), `wiki/{group}/proposal/` (wiki proposals). All verified against writers; rows
  added. Blackboard checked clean (signals + own WAL — no KV prefixes).
- **F5:** `docs/guide/building-on-mycelium.md` reserved-prefix list (the copyable integrator
  snippet — high-stakes) was missing the same nine → list updated to twenty prefixes.
- Verified clean: all 23 lock-table rows exist in code (incl. alias-hidden `GossipRxs`,
  `SenderLog`); cited gates exist (`dual_curators_reconcile_to_a_single_writer`,
  `shutdown_breaks_the_task_cycle_and_frees_the_wiki`, `alloc_port`, `env_test_lock`);
  `MAX_KV_WRITE_BYTES` formula, `WIRE_VERSION = 12` / `PREV = 11`, `swim_failure_detector:
  true` default; diagnosability endpoints on `dev/operations.md` all present in `http.rs`.

**Staleness:** none — today's artifact-library ingest (history/companions/.log/CLAUDE.md) was
done at ship time; no ✅-shipped work still described as pending anywhere in `docs/wiki/`.

**Orphans & dead links:** all relative links in `docs/wiki/` + both front-door guides resolve;
every folder has its folder-note.

**Coverage (F6):** the port-TOCTOU bind-retry lesson (today's CI flake fix) lived only in test
comments + a commit message → added to [testing.md §Ports](../testing/testing.md): retry
granularity follows topology (per-agent vs per-pair under mutual bootstrap), never bare-unwrap
a re-bind.

Pages touched: `concurrency/lock-order.md` (row 23 + scope note), `dev/operations.md`
(govern/timing), `testing/testing.md` (§Ports), plus code-canon `src/lib.rs` (namespace table)
and front-door `docs/guide/building-on-mycelium.md` (reserved prefixes).
