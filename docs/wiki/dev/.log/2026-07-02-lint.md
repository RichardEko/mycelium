## [2026-07-02] lint | founding pass — doc-vs-code sweep

First lint, run at wiki creation. Checks 1+3 executed (doc-vs-code + links); 2+4 n/a on
day one.

**Findings (4, all fixed in `concurrency/lock-order.md`):**
- Two unlisted lock sites: `HttpCtx::gateway_caps`, `HttpCtx::lock_guards` (gateway
  register/lock maps) → rows 15–16.
- `OidcVerifier::cache` is `tokio::sync::RwLock` with the write guard deliberately held
  across the JWKS fetch (single-flight refresh) — the "never tokio::sync" claim was too
  strong → row 17 + the sanctioned-exception note. Notably this predates the Run-28 table
  extension: even the *extended* table missed it, which is the argument for this check
  being mechanical.
- `signal_boundary` is `parking_lot::RwLock` (context.rs imports parking_lot), and rows
  1/3/4 moved to `CoreCtx` in the M1 split — owner/flavour cells corrected.

**Verified clean:** all 8 named regression gates exist; `WIRE_VERSION = 12` /
`PREV = 11` / `MAX_KV_WRITE_BYTES` / `swim_failure_detector: true` all match cited code;
all relative wiki links resolve.
