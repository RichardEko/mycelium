# 2026-07-15 — wiki-lint (9th pass; post philosophy port + artifact surfacing + Loads banner)

Diff since `lint-8`: the **philosophy.html → philosophy.md port** (retire the styled HTML, repoint 48
refs), the **artifact deploy/install surfacing** (guide ladder row + matrix focus), and the
**`## Loads` banner** across the 5 runtime-loading demos. Mostly docs + example code; no `src/`/
`mycelium-core/src/` change bar the `lib.rs` philosophy-path doc comment — so §1's code-verification
sub-checks were not triggered.

## §3 Orphans & dead links — clean
- **Philosophy port:** all 5 markdown links to `philosophy.md` resolve (`docs/guide/00·faq·README`,
  `docs/README`, `docs/wiki/domain/pattern-coverage`); the remaining ~40 references are prose backtick
  `docs/philosophy.md` (skills, plans, wiki.md, AGENTS.md, architecture.md, domain.md, lib.rs) needing
  only the file, which exists. **Zero** `philosophy.html` references remain anywhere (verified at port
  time; re-confirmed).
- **Artifact surfacing:** the guide "Artifacts & deploy" row's anchor
  (`operations/artifacts.md#solutiondev--authoring--publishing-an-artifact`) resolves (heading present;
  same anchor the cookbook already uses).
- The new §4 cross-links resolve (`examples.md` ↔ `ui-example-contract.md`; the latter → `coop/common/loads.rs`).

## §2 Staleness — clean
No stale "philosophy is styled HTML / a deck" framing survived the port (the two `ratings.md` "deck"
mentions are the *publications* decks, which remain HTML — not philosophy). No shipped-vs-planned drift.

## §4 Coverage — **1 finding, fixed**
The new **`## Loads` / `coop::common::announce_loads` banner** — a standardized convention across the 5
runtime-loading demos (`catalog · mcp_toolgrowth · provisioning · model_deploy · reheal_deploy`), the
CLI analogue of the browser concepts box, declaring **content · type · loaded-from** — was durable
knowledge with **no wiki home**. Filed it: a note in `examples.md` (the coop-suite description) and a
new "**The CLI analogue — the `Loads` banner**" section in `ui-example-contract.md` (where the
concepts-box standard lives), cross-linked. The artifact-install *docs* themselves are already covered
(`doc-coverage` rates Artifacts ✓; the surfacing improved HOW-Dev findability from the guide ladder).

No calibration entry — nothing a prior lint declared clean was found drifted.

## Pages touched
`docs/wiki/dev/examples.md` (Loads-banner note), `docs/wiki/dev/ui-example-contract.md` (CLI-analogue section).
