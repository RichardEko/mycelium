# 2026-07-06 — delivery-ledger ingest (session tail)

Extended `dev/history.md` Post-v2.0 section (now 2026-07-04/06) + refreshed the self-audit line:

- **Opacity control-signal-shed fix (#129)** — real liveness bug: `System`-scoped BOUNDARY_OPAQUE/
  TRANSPARENT were dropped by `deliver_locally`'s probabilistic shed under gossip backpressure; the
  "I'm shedding" signal shed by the shedding mechanism. Fixed (control kinds exempt from the local shed)
  + deterministic regression. Root-caused by a deliberate dig (analysis Run 37) after 3 latency-misdiagnoses.
- **v3.0 positioning (PROPOSED, not started)** — coordination space covered natively/by-composition; two
  primaries (`mycelium-reason` DX, `mycelium-guardrails` structural guardrails) + the three-tier LLM-DX
  strategy + LangGraph-checkpointer plan; RAG/HITL/content-guardrails = use-case functions via the mesh.
- **Analysis methodology upgraded (bright line at Run 37):** current-score = current-state; unknown-unknowns
  reserve + carried-score decay; never retro-rewrite past scores. Self-audit line: 36 → 37 runs, 27 ledger entries.

No page beyond `history.md`; folder-note chain unchanged.
