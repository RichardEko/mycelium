# 2026-07-09 — wiki lint 3 (dev section)

Scope: post-#151 (`subscribe_log_group` exact-once fix). Two doc-vs-code findings, both fixed;
load-bearing tables clean.

## §1 Doc-vs-code — 2 findings, fixed

- **F1 (Minor, fixed).** `dev/examples.md` cited `make test-integration`; that target does not
  exist (it is `make test`). The correction had been made only on the held #147 branch, so `main`
  was stale. Fixed to `make test` (4-node) + noted the `make test-overlay` overlay suite. Did
  **not** pull in #147's unreleased `cluster-suites.yml` reference (that workflow isn't on `main`).
- **F2 (Minor, fixed) — my own drift.** The `runtime-invariants.md` `subscribe_log_group` note
  (added mid-#151) described the *intermediate* design ("hold the group claim as a consensus lock
  for the whole session"), which the shipped fix superseded. Rewritten to the **merged** design:
  a **leased consensus claim with converged-holder confirmation** (`system_propose` commits
  optimistically against a node's *local* committed view → two proposers can both commit → confirm
  the winner via the LWW-by-HLC *converged* committed holder; losers stand by without releasing;
  winner drains a private offset + renews the lease → failover). Also captured the reusable lesson
  (a consensus lock is not mutually exclusive on the propose *return* — confirm via the converged
  committed value), which is genuine new invariant knowledge (§4 coverage).

## Clean

- **Lock-order table:** unchanged. Full-workspace `Mutex<`/`RwLock<` sweep = same inventory as the
  2026-07-09 lint; every site maps to rows 1–30. #151 **removed** `SubscribeHandle` (it held no
  `Mutex`/`RwLock` — its "lock" was a bare KV write), so no row to retire.
- **KV-namespace table** (`src/lib.rs`): unchanged. #151 moved the consumer-group *claim* from a
  `lock/clog/…/claim` bare-KV write to a **consensus slot** (`clog/…/claim` → committed under
  `consensus/committed/…`, covered by the existing `consensus/committed/{slot}` row); `clog/` is
  still the consumer-position offset; `lock/` is still used by `ConsensusHandle::distributed_lock`
  + the gateway lock endpoints. No new/invalidated top-level prefix.
- **Constants:** `WIRE_VERSION = 12` / `PREV_WIRE_VERSION = 11` (framing untouched by #151).
- **§3 orphans/dead links:** clean — every wiki relative link resolves; folder-notes present.
- **Staleness sweep** of merges since the last lint (#145 k8s/terraform, #146 examples standard,
  #148 plan decisions, #151): durable knowledge ingested (deploy multi-host → `scale-tests.md`;
  examples standard → `examples.md`; #151 → this note). `pattern-coverage.md:83`'s
  "durable/partitioned log … *would* push toward Kafka semantics" is forward-looking, not stale.

Pages touched: `dev/examples.md`, `dev/architecture/runtime-invariants.md`.
