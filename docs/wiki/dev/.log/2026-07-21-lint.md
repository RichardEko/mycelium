# 2026-07-21 — wiki-lint (diff pass over the cold-start-saga week)

Diff-gated against the clean 2026-07-20 full sweep; scope = the week's delta (capability
lease, cold-start fixes ×3 commits, CI-gate closure, coop-demo hardening).

## §1 Doc-vs-code — CLEAN
- No new `Mutex`/`RwLock` fields or KV-prefix writers in the delta (lock-order + namespace
  tables carry). Wire pins v12 / PREV 11 unchanged.
- All newly-cited gates exist (`test_cold_start_rpc_*` ×2, `became_alive_activates_*`,
  `test_gateway_cap_lease_*`, `failover_preserves_items_and_ids`, the loom broken twin).
- **Watch-RMW sweep** (first scheduled run of the new check): clean — one known
  false-positive shape (receiver-borrow relay, `capability_handle.rs`).
- **CI-gate block now fully live**: all three formerly-phantom gates have `run:` lines
  (closed `3a27655`, day after escalation). Closure note appended to the 2026-07-20 entry.

## §2 Staleness — one fix
The 2026-07-20 lint log read as if the CI gap were still open; closure note added.

## §3 Orphans & dead links — CLEAN (delta links: flake log, lock-free page, loom paths).

## §4 Coverage — one finding (fixed)
The week's new hot invariants (Ping-borne peer learning · every Ping carries peer exchange ·
union-sized activation caps · RMW-atomic watch publishes) lived only in the flake *log*
(history), not on a current-truth page. Added as corollaries under
[runtime-invariants § Fan-out activation](../architecture/runtime-invariants.md), with their
gates named. No calibration entry — no prior-pass miss surfaced (the invariants are newer
than the last pass).

Pages touched: `runtime-invariants.md` · `.log/2026-07-20-lint.md` (closure note) · this entry.
