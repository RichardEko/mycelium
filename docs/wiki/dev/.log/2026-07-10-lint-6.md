# 2026-07-10 — lint 6 (deck-driven, final)

Triggered by the presentation/customer-pitch realignment; scoped to catch drift the deck edits
touched. The decks are front-door docs that restate code facts, so §1 verified every deck claim
against code, and the sweep found a wiki coverage gap the lock work left.

## §1 Doc-vs-code — deck facts all verified, one self-citation fixed
- **Every code fact in the decks is correct against code:** `LockService` methods
  (`locks()`/`lock`/`with_lock`); fencing token = commit HLC; the four cited gateway endpoints
  (`/gateway/diagnose`, `/gateway/explain`, `/gateway/fleet`, `/gateway/overlay/lock/acquire`)
  all exist; 17 guide chapters (00–16); 14 coop bins / 12 `ci_smoke` demos (= "14 = 12 CI + 2
  manual"); Legible Emergence complete (phases 0–5). No deck drift.
- Lock-order table current (the only lock-shaped diff line is #163's `GcContext` reference field,
  already logged in lint-3). The three new lock gates exist.
- **Fixed my own bad citation** in the ingest below (`tests/failover.rs` → the gates are actually
  in `src/agent/overlay_consistent.rs` + `src/agent/lock_service.rs`) — caught by the §3 path check.

## §4 Coverage — 1 finding, fixed (the real one)
The distributed-lock work (#164/#165/#166 + the `LockService`) had **no durable wiki home** —
only a dated log mention. Ingested into `runtime-invariants.md` as a section under the
converged-holder lesson (`distributed_lock` was that lesson *failing in shipping code*, found by
probes), plus the new reusable invariant: **a monotonic fencing token must be the commit HLC, not
the ballot** (the ballot regresses under gossip lag — #166's example finding). Gates + the runnable
`examples/distributed_lock.rs` cited.

## §2 Staleness / §3 Links — clean
No stale wiki claim about locks (grep found none but the log entry). Self-anchors resolve; the
code-file citations in the new section exist (after the path fix).

Pages touched: `dev/architecture/runtime-invariants.md`.
