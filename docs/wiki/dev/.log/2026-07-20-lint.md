# 2026-07-20 — wiki-lint (post-v2.2.0, lease-feature session)

## §1 Doc-vs-code
- **Lock-order table: CLEAN** — all 30 rows verified against a whole-workspace sweep (incl.
  both lock-wrapping aliases `GossipRxs`/`SenderLog`); no undeclared sites, no dead rows.
- **Wire-version pins: CLEAN** — every pinning page states v12 / PREV 11 (guide chapters included).
- **Named test gates: CLEAN** — every wiki-cited test name exists (three grep hits were prose
  substrings, not citations).
- **KV-namespace table: TWO MISSES (fixed)** — `facts/{node}/{field}` (agentfacts CRDT) and
  `manifest/…` (`mesh_manifest::manifest_keys`) were live but absent from `src/lib.rs`'s table and
  the guide's reserved list; both added, plus `sys/health` / `sys/rate` / `sys/role` subkey rows.
  Repeat of the 2026-07-07 family → calibration-ledger entry (sharpening: grep prefix *constants*
  and `*_keys` modules, not just `format!` literals).
- **CI-gate list: THREE PHANTOM GATES (reported as CI gap, page NOT patched)** — `testing.md`'s
  "full CI gate" block lists `cargo test --lib --features compliance` (never in any workflow),
  `cargo test --lib --no-default-features --features gateway` (never), and
  `cargo clippy -p mycelium-core --lib --tests` (comment-only since 979f8d6). All three run in
  `make check`, so local green masks it — the make-check-vs-CI-green family (2026-07-16 ingest).
  Escalated to the operator as a **code bug** (add the steps to ci.yml) per the
  report-don't-paper-over rule; calibration-ledger entry appended (2026-07-13/14/15 passes had the
  sharpened check and missed it).
- **operations.md feature list: fixed** — added `otel`; noted `fuzz-internals`/`test-util` as
  deliberately unlisted test-only features.

## §2 Staleness
Clean. v2.2.0 has its history entry; the 2026-07-16 make-check-full lesson and the 2026-07-20
gateway-capability-lease work are both ingested; ops-console KV-tab fix is a tool fix, no ingest
needed.

## §3 Orphans & dead links
Clean — folder-note chain complete, all wiki + front-door relative links resolve, inbound
`examples/README.md#anchor` sweep clean (two non-link prose hits in an old log entry).

## §4 Coverage
- **UI-example contract: CLEAN, fully reconciled** — 12 `include_str! html` hits = 10 compliant
  (all verified to carry `ui/viz` + `__CONCEPTS__` + `__OPS_CONSOLE_LINK__`) + 3 documented
  exceptions (`conway-gpu` · `three_node_demo` [inline HTML, not in grep] · `ops_console`).
- **Examples enumeration: one pending item (user decision)** — `mycelium-tuple-space/examples/`
  gained `scrape_fleet_node.rs` / `scrape_worker_node.rs` (uncommitted, deployment launchers for
  the analytics scraper fleet). Absent from the capability matrix and the crate's examples README.
  Deliberately NOT added: the files are untracked WIP referencing an external repo's runbook —
  classify (matrix rows vs. documented deployment-utility exception) when they're committed.

## Pages touched
`docs/wiki/dev/operations.md` (otel + earlier same-day lease para) · `src/lib.rs` (namespace
table, +5 rows) · `docs/guide/building-on-mycelium.md` (reserved list) ·
`.log/lint-calibration.md` (+2 entries) · this entry.
