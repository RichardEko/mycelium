# 2026-07-15 — v2.1.0 released (docs updated)

Cut **v2.1.0** (tag `v2.1.0`, commit on `main`) — the first MINOR since v2.0.0 (2026-07-04). Wire
**v12** (PREV 11) unchanged → backwards-compatible rolling upgrade. Release gate `make check-full`
green (clippy feature-matrix + wasm-host clippy + 794 tests, 0 failed). Tag-only (no crates.io).

Substrate highlights (accumulated in `[Unreleased]` since v2.0.0, cut to `CHANGELOG` § [2.1.0]):
`LockService` (`agent.consensus().locks()` — blocking acquire + scoped `with_lock`, monotonic-HLC
fencing token); the **#164 distributed-lock Critical fixes** (mutual exclusion + releasability);
`connect_peer`/`disconnect_peer`; CI-gated Docker cluster suites; plus the self-targeted-signal-flood
/ tuple-space discovery-wait + late-secondary-backfill / HTTP `SO_REUSEADDR` fixes. This session's
delta was examples/docs only (faceted capability matrix, two artifact browser showcases, `## Loads`
banner, UI-example contract, `philosophy.md`) — no substrate change (see analysis Run 49).

Version state updated in: `docs/wiki/wiki.md` (the anchor), `docs/wiki/dev/history.md` (the ledger,
full v2.1.0 section), `ROADMAP.md` (Status line), `CLAUDE.md` (on-ramp). 7 workspace crates bumped
2.0.0→2.1.0; `mycelium-reason`/`mycelium-guardrails` stay on their independent 0.1.0 track.
