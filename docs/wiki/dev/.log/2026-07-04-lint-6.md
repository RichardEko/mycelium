# 2026-07-04 — lint pass (post bincode-retirement + ops checklists)

Scope: two commits since [lint-5](2026-07-04-lint-5.md) — `49ccbf4` (fully retire bincode → golden
vectors) and `99bb9a2` (production-readiness + customer-pilot checklists). **One coverage fix.**

**1 · doc-vs-code** — clean.
- The bincode retirement removed `WireMessageV11`, `bincode_cfg`, and renamed several codec/serde tests
  (`codec_matches_bincode_byte_for_byte` → `…_golden_wire_bytes`, `primitives_match_bincode` →
  `primitives_round_trip`, etc.). **No wiki page cited any of the removed/renamed symbols** — grep clean.
- `dev/history.md`'s "M11 codec (bincode retired, RUSTSEC-2025-0141)" remains accurate (bincode is now
  fully out of the tree, not just off the wire — the wording needs no change).
- **Lock-order table unaffected:** precise in-scope field-decl count is 6 + 2 known type aliases
  (`GossipRxs`, `SenderLog` — both already in the table); `49ccbf4` added **zero** locks (its signal.rs
  hunk was a doc comment). The crude 27-vs-19 first-pass count was over-counting (method-local locks,
  generics), not drift.

**2 · staleness** — one fix. `dev/operations.md`'s runbook reference listed the operations docs but
predated the two new checklists; added `production-readiness.md` (go-live pre-flight) +
`customer-pilot.md` (first-engagement scoping). No page describes a shipped item as pending.

**3 · orphans & dead links** — clean. New relative links (`../../operations/{production-readiness,
customer-pilot}.md`) resolve.

**4 · coverage** — the bincode full-retirement is captured by `history.md`'s existing M11 line; the new
operations checklists now have a wiki reference. The checklists themselves live under `docs/operations/`
(operator runbooks), correctly outside the LLM wiki's synthesis layer.

Pages touched this pass: `dev/operations.md` (added the two checklists to the runbook reference). No code
bug surfaced.
