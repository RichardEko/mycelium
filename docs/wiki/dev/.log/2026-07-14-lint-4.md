# 2026-07-14 — wiki-lint (4th pass today; post examples-audit + FAQ edits)

**One finding, fixed.** Scoped to the large delta since `2026-07-14-lint-3` (baseline `e542401`):
the examples audit (orphan `mesh_demo.html` deleted, `diagnostics` registered, `conway-gpu` palette,
`three_node_demo`+`llm_agent` re-themed dark + given `ui/viz` ops-linking) and the FAQ/guide edits
(langgraph citation, ch15↔FAQ crosslink, broker-bullet de-jargon). No product-code change in the
delta (`git diff --stat e542401..HEAD -- src/ mycelium-*/src/` empty), so §1's core checks
(lock-order, wire, KV namespace) were unaffected.

## §1 Doc-vs-code
Clean:
- No substrate change → lock/wire/KV constants untouched.
- `dev/examples.md` has **no dead ref to the deleted `mesh_demo`**.
- The example port facts stated in the fix below verified against code: `three_node_demo`
  `CHAT_PORT_DEFAULT=8080` / `MGMT_PORT_DEFAULT=8090`; `llm_agent` `HTTP_PORT_N0=8100`,
  `GW_PORT_N0..N2=9100..9102`.

## §2 Staleness
Clean: `diagnostics` (newly registered coop `[[bin]]`) is enumerated in `examples.md` (coop-suite
line); `conway-gpu` still listed; the deleted `mesh_demo` left no stale mention.

## §3 Orphans & dead links
Clean: the three new cross-doc links resolve — `faq.md → ../../examples/langgraph/` (dir exists);
`15-reasoning-and-langgraph.md → faq.md#why-not-langgraph--temporal--nats--` (anchor slug matches the
one `faq.md` uses for its own internal link); the FAQ broker edit added no links. The FAQ prose edits
restate *positioning*, not code facts, so no front-door code-fact drift (§1) introduced.

## §4 Coverage — the finding (by-category check, per the 2026-07-14 ledger sharpening)
`examples.md`'s Ops-Console `ui/viz` note read *"each **showcase** self-advertises its browser UI"* —
accurate before this session, but the browser-UI surface now spans **two categories**: the animated
**visual showcases** (enumerated) **and** the **browser operator demos** — `three_node_demo` (chat +
mgmt UIs) and `llm_agent` (Mesh Control) — which this session re-themed dark and gave `ui/viz`
two-way linking. The note's showcase-only lead under-framed that second category (though its generic
"any gateway example with a UI opts in" clause technically covered it). **Fixed:** the lead now reads
"every browser demo," and the sentence names the two operator demos + their ports. This is the
sharpened category-completeness check working on drift *this session introduced* — **not** a
prior-lint miss (the two demos weren't ops-linked at lint-3 time), so **no calibration-ledger entry**.

**Pages touched:** `docs/wiki/dev/examples.md`. **User decisions:** none.
