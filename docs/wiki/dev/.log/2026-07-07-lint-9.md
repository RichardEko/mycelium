# 2026-07-07 — lint 9 (post profile-as-artifact)

Narrow-window pass: one commit since lint 8 (`5ca95c9`, model_deploy v2 — the deployment
profile as a governed artifact, design §4.3.1). One finding, fixed.

**Doc-vs-code:** clean — lock table complete by name-diff (44 sites, 2 known aliases; no new
locks in the commit), no new KV prefixes (the demo writes only tabled ones), `WIRE_VERSION =
12` / `PREV = 11` confirmed.

**Staleness (F1):** [examples.md](../examples.md)'s `model_deploy` line predated v2 —
described the GGUF but not the governed profile. Updated (weights **and** profile as two
signed artifacts, profile → weights by content address, tokens generated under the governed
profile). The history ledger and companions page needed nothing: `5ca95c9` ingested itself at
ship time (the pattern to keep — ship-time ingest makes lint passes cheap).

**Orphans & dead links:** all resolve (wiki + front doors + coop README + the design note).

**Coverage:** §4.3.1's knowledge (composite deployments, content-address references between
artifacts, failed-activation-retry as the mechanism-free ordering) lives in the design note +
history ledger; no homeless knowledge.

Pages touched: `examples.md` only.
