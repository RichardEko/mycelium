# 2026-07-15 — wiki-lint (8th pass; post README Demos→nav)

**Clean — no findings.** Diff since `lint-7`: a single root-`README.md` change (the `## Demos` table
replaced by an *Examples* row in *Where next?*, Docker one-liners relocated to `## Run`). No `src/`,
`mycelium-*/src/`, or `docs/wiki/` change — so §1 (doc-vs-code) and §2 (staleness) were not triggered.

## §3 Orphans & dead links — clean (the one real risk, verified)
Removing `## Demos` deletes the `#demos` anchor. Swept for inbound links (`README.md#demos` /
`](#demos)`) across `docs/`, `examples/`, `mycelium-*/`, `README.md` — **none**. This is clean *because*
`llm_agent`'s matrix link had already been repointed off `../README.md#demos` (→ guide ch02) in the
earlier off-page-links commit; the section removal therefore stranded no anchor. All the README's own
outbound links resolve (`examples/README.md`, faq, building-on, guide, chat, operations).

## §4 Coverage — clean
No new example/category/handle; the relocated `make test-{llm-agent,llm-demo,overlay}` one-liners keep
the overlay demo reachable (the dropped "Consistency Overlay" table row was pure duplication of
`tests/overlay/`). `examples/README.md § The capability matrix` remains the single source of truth the
root README now points to rather than races.

No calibration entry — nothing a prior lint declared clean was found drifted.
