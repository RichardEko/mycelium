# 2026-07-14 — wiki-lint (3rd pass today; post cluster_name policy)

**Clean pass — no findings.** Scoped to the delta since `2026-07-14-lint-2` (baseline `4c7afa7`):
the **cluster_name policy** across all examples (`f5c7f6c`, `553413e` — every example now sets a
cluster name via the standard `Some(env::var("GOSSIP_CLUSTER_NAME").unwrap_or("…"))` pattern) plus
the `guide/13-cluster-topology.md` env-var-gotcha fix, and the run-5 analysis/doc-coverage/
publication-lint artifacts. No product-code change in the delta.

## §1 Doc-vs-code
Clean:
- **Lock-order table unaffected** — `git diff --stat 4c7afa7..HEAD` over `src/`·`mycelium-*/src/`
  is empty; the whole delta is `examples/` + `docs/`. Product-lock count unchanged (70).
- **Wire constants untouched** — no `framing.rs` change; the one changed guide chapter
  (`13-cluster-topology.md`) pins no `WIRE_VERSION`, so the guide-chapter constant grep has nothing
  to diff.
- **KV-namespace table unaffected** — examples add no new KV prefix writers.
- **`cluster_name` mentions verified accurate** — `operations.md:11` (the `/stats` field, sourced
  from `GOSSIP_CLUSTER_NAME`, `cluster` metric label, AgentFacts field) and
  `deployment-framing.md:10` (label-not-boundary) both still match code; `operations.md` correctly
  calls it *optional*.

## §2 Staleness
Clean — no wiki page describes examples/config as leaving `cluster_name` unset/null (the grep for
`cluster_name … null|unset|not set` returns nothing), so the "all examples now set it" policy
introduces no contradiction.

## §3 Orphans & dead links
N/A this delta — no wiki page renamed/moved; front-door docs (`building-on-mycelium.md`, `faq.md`)
untouched.

## §4 Coverage — the notable positive
The doc-coverage **run 5** calibration hit was: *`Membership+cluster_name·HOW·Dev` was ✓ but the
guide's `GOSSIP_CLUSTER_NAME=…` instruction omitted that env vars require
`cfg.apply_env_overrides()`.* Checked whether the **wiki** carries the same drift — it does **not**:
`dev/testing/scale-tests.md:55-59` already documents the *general* form of exactly this gotcha ("the
Docker demo built its config from `GossipConfig::default()` and never called `apply_env_overrides()`
— so … every `GOSSIP_*` knob was silently ignored"). So the run-5 finding had **no wiki analogue** —
the *guide chapter* was the gap (now fixed, `15a33eb`); the wiki was ahead. No coverage finding, and
per the skill's "clean pass surfaced no miss" rule, **no calibration-ledger entry** (this was not a
prior-lint miss — the gotcha is instruction-completeness in a guide chapter, which is doc-coverage's
remit, not wiki-lint's doc-vs-code-fact remit; and doc-coverage already folded the sharpening).

**Pages touched:** none (clean). **User decisions:** none.
