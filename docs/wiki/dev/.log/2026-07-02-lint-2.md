## [2026-07-02] lint | second pass (full /wiki-lint run)

**Check 1 (doc-vs-code) — 2 findings, fixed:**
- `SenderLog` (`mycelium-core/src/signal.rs:134`) — per-kind `parking_lot::Mutex<VecDeque>`
  values inside a papaya map, hidden from the field grep by a **type alias** → row 18 in
  `concurrency/lock-order.md`; the alias grep added to the `/wiki-lint` procedure. Usage
  verified retry-safe (arc via `compute`) + leaf + never across `await`.
- `cli` feature missing from `operations.md`'s gate list → added.
Verified clean: all remaining table rows 1–17 map 1:1 to declarations (`gossip_rxs`
resolves via the `GossipRxs` alias); all named gates exist; `INDEX_STRIPES = 64`; all six
`/stats` fields present in `http.rs`; feature list matches `Cargo.toml`.

**Check 2 (staleness):** clean — only the wiki-adoption commit and the already-ingested
Run-28 fix commits since the founding pass.

**Check 3 (orphans/links):** clean — all relative links resolve; every page reachable;
every folder has its folder-note.

**Check 4 (coverage) — 1 finding, fixed:** the three-arm work-distribution experiment
(the one remaining research-track item) had no wiki home → section added to
`domain/publications.md` (plan/runner/data paths verified to exist).
