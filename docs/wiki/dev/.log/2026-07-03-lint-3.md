# 2026-07-03 — lint pass (post mycelium-wiki gateway + SDKs)

Scope: the mycelium-wiki workstream landed today (phases 1–5 + the Phase-4 gateway/SDK remainder,
`93b7b76`…`7b2f1e3`). Verified the fresh ingest against code.

**1 · doc-vs-code** — clean.
- Lock-order table: this session's code changes are confined to `mycelium-wiki/` + the TS/py SDKs —
  **out of the table's `src/` + `mycelium-core/src/` scope**, and the in-scope locks were verified in
  this morning's [lint](2026-07-03-lint.md) / [lint-2](2026-07-03-lint-2.md). No new in-scope lock.
- Companion page [companions/wiki.md](../companions/wiki.md) citations all resolve: `tests/failover.rs`,
  `tests/gateway.rs`, `ci_smoke.sh` exist; features `control-plane`/`llm`/`gateway` match
  `mycelium-wiki/Cargo.toml`; capability names (`wiki.{group}.curator`), KV prefixes
  (`wiki/{group}/proposal/`), and endpoints (`/gateway/wiki/{read,query,propose}`) match `src/`.

**2 · staleness** — one nit fixed. The same-day ingest entry
[phases-1-5-shipped](2026-07-03-mycelium-wiki-phases-1-5-shipped.md) was written **before** the
Phase-4 gateway/SDK remainder (`21917e0`) and still listed it as an open remainder; struck through with
a same-day-closed note. No other page carried stale "proposed/planned" framing for the wiki (grep
clean). All commits since lint-2 are the mycelium-wiki phases, each ingested.

**3 · orphans & dead links** — clean. `companions/wiki.md` is reachable from
[companions](../companions/companions.md) (added to both the bullet list and the taxonomy note); every
relative link in the two changed pages resolves.

**4 · coverage** — the workstream now has a home (`companions/wiki.md` + this section's `.log`). No
other durable knowledge is uncovered.

Pages touched this pass: the phases-1-5 ingest entry (remainder line). No code bug surfaced.
