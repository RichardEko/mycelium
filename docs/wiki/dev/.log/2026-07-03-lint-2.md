## [2026-07-03] lint | dev/ — clean, no findings (post explain-cap + Run 30)

Second lint of the day. Diff since the morning pass (`d1974c7`): two commits — the Run 30 (M2)
analysis (`74c637e`) and the explain fan-out cap (`16c3039`, the Run-30 Scalability finding fix).
The cap commit updated the wiki inline, so this pass finds nothing to fix.

- **Check 1 (doc-vs-code):** no new `Mutex`/`RwLock` fields since the last lint (diff empty) → the
  lock-order table stays executably complete (21 sites → 19 rows, verified in full this morning).
  New citations verified against code: `EXPLAIN_MAX_FANOUT = 32` (matches
  `src/agent/emergent.rs:449`), `select_explain_targets_caps_the_fanout_and_names_the_remainder`
  (exists), the `not_queried` field (cited + present). All other cited gates
  (`test_explain_fanout_…`, `test_fleet_diagnosis_…`, `test_fleet_snapshot_agrees_…`, `test_p1_…`)
  still exist — the only grep "misses" are line-wrapped fragments of names that continue on the next
  line, confirmed present in full.
- **Check 2 (staleness):** no stale "fans out to every peer / uncapped" language remains — the cap
  wording (`diagnostics.md`, `operations.md`) replaced it; no page describes the Scalability finding
  as open.
- **Check 3 (orphans/links):** every folder has its folder-note; every relative link across the
  wiki resolves.
- **Check 4 (coverage):** the cap has a `.log` entry + lives in `diagnostics.md`/`operations.md`;
  the Run 30 analysis + its three kept falsification probes live in `docs/analysis/ratings.md` +
  `src/lib_tests.rs` (their correct homes — the wiki does not track analysis runs).

No pages edited. No code bugs surfaced. No user decision needed.
