# 2026-07-10 — lint 7 (post-scope-rename + group-docs)

Delta: the System→Cluster rename (#167) + the group define/monitor/why docs + CLAUDE.md note.

## §1 Doc-vs-code — 1 finding, fixed
- **Stale example:** `guide/10-language-bridges.md` had a TS snippet `emit(..., { scope: "system" })`.
  Still *works* (deprecated alias), but examples should be current → `"cluster"`. Fixed (swept all
  guide/ops `.md` for `scope: "system"` / `scope="system"`; this was the only one).
- Code symbols the renamed docs cite exist (`cluster_propose`, `SignalScope::Cluster`). Wiki is
  clean of stale `SignalScope::System` / `system_propose` (the rename swept runtime-invariants +
  testing.md). Lock-order table current (no new locks). The `13-cluster-topology.md:51` "Note on
  system" hit is the *intentional* rename history, not stale — left.

## §2/§3 — clean
No broken relative links across wiki + front-doors + touched docs. Anchor-fragment check clean
after directly confirming one false-positive: `cookbook → README#which-crate--mycelium-vs-mycelium-core`
**resolves** (GitHub keeps the double-hyphen from "? —"; my ad-hoc checker mis-flagged the em-dash
heading — the link is correct and was NOT changed).

## §4 Coverage — clean
The rename has a dated `.log` (2026-07-10-scope-rename). Group define/monitor + governed-vs-
ungoverned + why-groups are guide-level (cookbook/00-concepts — the right home for how-to +
vocabulary; the wiki cites the guide, doesn't duplicate). No wiki gap.

Pages touched: `guide/10-language-bridges.md`.
