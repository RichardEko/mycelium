# 2026-07-03 — lint pass (post Run-32 remediation)

Scope: the two Run-32 fixes landed since [lint-3](2026-07-03-lint-3.md) —
`23325a9` `Wiki::shutdown` (the task/`Arc` cycle finding) and `7dc8b04` the change-driven lint (the
Scalability finding) — plus their companion-page notes. **Clean.**

**1 · doc-vs-code** — clean. Every symbol the companion page now cites resolves in
`mycelium-wiki/src/`: `Wiki::shutdown`, `lint_pass_count`, the `lint_dirty` flag (6 refs), the canary
`shutdown_breaks_the_task_cycle_and_frees_the_wiki`. Capability names (`wiki.{group}.curator` /
`.candidate`), KV prefix (`wiki/{group}/proposal`), and gateway endpoints
(`/gateway/wiki/{read,query,propose}`) all match code. Lock-order table: this session's locks are in
`mycelium-wiki/` (out of the table's `src/` + `mycelium-core/src/` scope); no in-scope lock added.

**2 · staleness** — clean. The companion page carries both fixes (the `Wiki::shutdown` lifecycle rule +
the change-driven lint). Per M2 the Run-32 audit entry in `ratings.md` is left immutable: its
Resource-Management row already reads "Fixed same run"; its Scalability row ("flagged not fixed") was
accurate at write time and is **addressed post-run in `7dc8b04`** — that resolution belongs to Run 33's
re-score, not a retro-edit of Run 32.

**3 · orphans & dead links** — clean. All relative links in the touched pages resolve.

**4 · coverage** — the Run-32 findings and their fixes have a home (companion page + `ratings.md` Run 32
+ the fix commits, each with a canary that flips on regression). Nothing uncovered.

Pages touched this pass: none (verification-only; the fixes were ingested with their commits). No code
bug surfaced.
