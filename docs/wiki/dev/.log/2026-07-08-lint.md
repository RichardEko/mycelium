# 2026-07-08 — wiki-lint (post-mycelium-reason)

First lint after the `mycelium-reason` companion + Python tier merged (#130/#131). Doc-vs-code
was the load-bearing pass; three findings fixed, all doc edits (no code bugs).

## §1 doc-vs-code — 3 findings fixed

- **F1 (real drift, load-bearing) — the `{ns}/loading` capability attribute is `pct`, not
  `progress`.** The canonical KV-namespace table (`src/lib.rs` §KV namespace ownership) documented
  the `cap/{node}/llm/loading` + `cap/{node}/{ns}/loading` progress attr as `progress`. The **shipped**
  writer — `mycelium-wasm-host`'s `Provisioner` (`provisioner.rs:375`) — writes `pct`; the only
  `progress` writer is the `llm_agent` example's explicitly-**SIMULATED** pull. `mycelium-reason`'s
  `ModelDependency::loading_progress` (the real consumer) correctly reads `pct`, so a consumer written
  from the doc would never match. Fixed both table rows to name `pct` as the shipped attribute (noting
  the example's `progress` simulation). Verified: no non-example `progress` writer exists.
- **F2 — KV-namespace table missing the new companion's prefixes.** The table itemizes companion
  prefixes (`wiki/`, `tuple/`, `installable/`, `comp/`) but not `mycelium-reason`'s. Added rows:
  `cap/{node}/llm/{model}`, `cap/{node}/llm-meta/{model}`, `cap/{node}/reason/blob-cache`,
  `log/reason/{run_id}/{node}/{hlc}`, and (Python saver) `ckpt/{…}` + `ckptw/{…}`.
- **F3 — front-door reserved-prefix list drifted from the table** (same class lint-7 flagged for
  `building-on-mycelium.md`). The integrator "do not write under these" list held 12 prefixes; the
  `src/lib.rs` table's top-level set has 9 more (`svc/ log/ clog/ lock/ prompts/ skills/ installable/
  comp/ wiki/`). `log/` especially is a trap — `KvHandle::append` writes `log/{stream}/…`. Synced the
  sets and split substrate-owned vs companion-owned, with an explicit `log/`-append caveat.

*Checked clean:* zero new locks in `mycelium-reason` (lock-order table still 30 rows — matches the PR
claim); no lock-wrapping type aliases beyond the known `GossipRxs`/`SenderLog`; `WIRE_VERSION = 12` /
`PREV = 11` (building-on-mycelium's "currently 12" correct); the eight sub-handle names
(`kv/mesh/capabilities/consensus/service/schemas/llm/mcp`) all exist. Note: `cap/{node}/llm/inference`
(table line 105) is real — the `llm_agent` example's config-probe convention — and distinct from
`mycelium-reason`'s `llm/{model}` serving convention; the new rows disambiguate.

## §2 staleness — clean

The mycelium-reason ledger/positioning/roadmap were updated in the same session (commits `ab6a3a8`,
`1513c8b`); no ✅-shipped item still described as planned.

## §4 coverage — 1 gap filled

`mycelium-reason` had no home in `dev/companions/companions.md` (which enumerates all companion crates).
Added an inline entry (matching the wasm-host/agentfacts density) flagging it as the **different axis**
(LLM-authoring DX, not coordination) with the three wedges + blob tier + Python tier.

## §3 orphans / dead links — clean

All links in the edited/ingested pages (`companions.md`, `history.md`, `pattern-coverage.md`, this
session's `.log`, and both front-door guides) resolve.

**Pages touched:** `src/lib.rs` (KV-namespace table — code canon), `docs/guide/building-on-mycelium.md`
(reserved list), `docs/wiki/dev/companions/companions.md` (new entry).
