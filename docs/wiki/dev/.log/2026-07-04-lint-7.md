# 2026-07-04 — lint pass (post v2.0.0 release + paper1 related-work + dev FAQ)

Scope: commits since [lint-6](2026-07-04-lint-6.md) — Run 33 (analysis), `3a4d82e` (v2.0.0
release), the paper1 related-work scan (`941024c`…`9f9da99`, 7 commits), and `8023c3a` (dev
FAQ + tuple-space example). **2 coverage fixes + 1 staleness nit** (this file's scope; the
domain-section publications fix is logged in `domain/.log/2026-07-04-lint.md`).

**1 · doc-vs-code** — clean.
- **Wire version:** `framing.rs` `WIRE_VERSION = 12`, `PREV_WIRE_VERSION = 11` — matches
  `wiki.md` and `dev/architecture/layers-and-crates.md` ("Merkle-bucketed since wire v12").
- **Lock-order table unaffected:** no lock-bearing code merged since lint-6 — the interval is
  analysis + release (Cargo.toml version bumps) + docs + one example (`redistribution.rs`,
  no `Mutex`/`RwLock` fields). Table carries clean.
- Bibkey `pressure-fields-decay` resolves in `paper1/references.bib` (1 hit).

**2 · staleness** — 1 nit fixed.
- `wiki.md` preamble said "v2.0 complete … v1.x production-readiness workstreams shipped" but
  never noted the actual **v2.0.0 release** (tag `v2.0.0`, `3a4d82e`, unified workspace
  version train). Updated the Version-state line. Run 33 needed no ingest (0 findings, no new
  invariant).

**3 · orphans & dead links** — clean. Edits added code-span canonical paths (page convention),
no new markdown links; reachability unchanged.

**4 · coverage** — 1 fix (dev section).
- `dev/companions/tuple-space.md` cited **no worked example** while `blackboard.md` cites
  `microgrid` and `wiki.md` cites `wiki_chat`. The new `redistribution` example
  (`8023c3a` — staged `intake→sorted→routed`, single-copy `take` + atomic `complete`,
  exactly-once assertion) gives tuple-space equal footing; added to its Gates line.
- The dev **FAQ** (`docs/guide/faq.md`) is a newcomer front-door in the guide, not the
  maintainer-facing wiki — deliberately **not** mirrored here (would over-couple). `dev/
  examples.md` indexes cross-cutting suites, not per-companion examples, so it is not stale.
