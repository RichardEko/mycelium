# 2026-07-15 — wiki-lint (2nd pass; post wiki_council_viz showcase)

**Clean pass — no findings.** Scoped to the delta since `2026-07-15-lint` (baseline `a29e8d7`): the
new `wiki_council_viz` browser showcase (`mycelium-wiki/examples/`, `:8095`) + its corpus, and the
doc-coverage run-7 edit (`09-security.md` "See it live" pointer). No product-code change
(`git diff --stat a29e8d7..HEAD -- src/ mycelium-*/src/` empty — the addition is an example + Cargo
registration; the `mycelium-wiki` lib is untouched), so §1's core checks (lock-order, wire, KV) were
unaffected.

## §1 Doc-vs-code
Clean: example registered as `[[example]]` with `required-features = ["gateway"]`; the ports the docs
cite (`:8095` dashboard) match `HTTP_PORT`/`OPS_PORT` in the source.

## §3 Links
Clean: the README showcase link `../mycelium-wiki/examples/wiki_council_viz.rs` resolves.

## §4 Coverage — proactive, no finding
`wiki_council_viz` is present in **all three** enumeration surfaces — `examples/README.md` (Browser
showcases table + by-layer companions note), `dev/examples.md` (the **Wiki companion** category, which
now lists both `wiki_chat` and `wiki_council_viz`), and the `examples-layer-matrix` (43 rows). This is
the category-completeness discipline (ledger 2026-07-15: derive categories from the tree; index a new
example at authoring time) applied *before* the lint rather than caught by it — the point of the
sharpening. No calibration entry (a brand-new example, not a prior-lint miss).

**Pages touched:** none (clean). **User decisions:** none.
