# 2026-07-26 — wiki-lint

Diff-gated over the delta since the 2026-07-24 lint (which covered the whole v2.3.0 SOC 2 arc). New
commits: `1c45b01` v2.3.0 (already lint-covered 07-24), `8bace1c` companion re-versioning, `4ecc1aa`
install-story fix. So the live delta was this session's two doc/version commits.

## §1 Doc-vs-code — CLEAN (carried + spot-verified)
- **Lock-order table:** 31 rows, complete. Both lock-wrapping aliases covered (`gossip_rxs` = row 5,
  `SenderLog` = row 18, WS-F `SubjectKeyRegistry` = row 31). No new lock field in the delta (no code
  change).
- **Watch-RMW sweep:** clean (no `borrow()`+`send()` in the delta).
- **Wire version:** `framing.rs` `WIRE_VERSION=12`/`PREV=11`; the four guide chapters that pin it
  (`09-security`, `12-schema-lifecycle`, `building-on`, `faq`) all read v12/11. The old `09-security`
  v10 drift stays fixed.
- **KV-namespace table:** clean — the five SOC 2 `sys/*` prefixes have `src/lib.rs` rows (added 07-24);
  no new prefix in the delta.
- **Front-door reserved list / sub-handles / features:** unchanged by the delta (the install-story fix
  touched only the dependency snippet).

## §2 Staleness — CLEAN
No ✅→pending drift. Companions still correctly marked COMPLETE.

## §3 Orphans & dead links — CLEAN
The `building-on` edit changed no headings (only the §1 dependency block), so no inbound anchors moved.
The three new relative links (companions→building-on, companions→history, history→building-on) all
resolve.

## §4 Coverage — ONE GAP (fixed)
This session's two durable facts had **no wiki home**:
1. **Companion re-versioning** — `mycelium-guardrails` 0.1.0 → **1.0.0** (API-freeze; scope
   feature-complete), `mycelium-reason` 0.1.0 → **0.5.0** (mature, pre-freeze), on independent version
   lines; "v3.0" = epoch, not version.
2. **Distribution reality** — the workspace ships **by git tag, not crates.io** (the `mycelium`/
   `mycelium-core` names are held by an unrelated dormant 2019 project; no reclaim path). The wiki
   never mentioned distribution at all.

Fixed: a *Versioning & distribution* note on `companions.md` (citing `building-on-mycelium.md` §1 for
the install snippets rather than duplicating them), and a dated *Companion re-versioning + distribution
reality* entry in `dev/history.md`.

## Calibration
Appended one entry (2026-07-26): §1's front-door check verified `WIRE_VERSION`/sub-handles/features/
reserved-prefixes for `building-on-mycelium.md` but **never that the dependency line resolves to this
crate** — so `mycelium = "2"` (which resolves to the 2019 crates.io collision) sat clean through every
pass. Sharpening folded in: the front-door check now verifies install/dependency snippets point at this
project. Same family as the 2026-07-20 "reserved by definition, not by writer" miss — a documented
fact about the outside world the check never cross-verified.

## Pages touched
`dev/companions/companions.md`, `dev/history.md`, `dev/.log/lint-calibration.md`, this log.
