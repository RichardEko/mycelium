# 2026-07-10 — lint 2 (delta pass after Run 41 + #162)

## §1 Doc-vs-code
Clean: no new lock sites since lint 10 (#162's `self_id` is a param, not shared state); all
four newly cited gates exist (`self_targeted_signal_does_not_flood`,
`client_ops_wait_for_discovery_under_default_config`,
`state_chunk_paginates_without_loss_or_duplication`, `connect_peer_racing_shutdown_is_safe`);
`WIRE_VERSION 12`/`PREV 11` unchanged; no new KV prefix writers.

## §2 Staleness — 3 findings, fixed
- **CLAUDE.md hot invariant** ("Individual-scope forwarding is unconditional") didn't know
  the #162 terminal carve-out — a contributor diffing the new `target == self_id` check
  against the on-ramp would see a contradiction. Added the one-clause carve-out with the
  explicit not-precedent framing.
- **cluster-suites.md** described the ERR trap without the errtrace lesson — `set -o
  errtrace` is load-bearing (without it the trap is not inherited by shell functions; the
  undiagnosable AFN failure, #161). Updated; "any new harness lib must set both."
- **tuple-space.md** lacked the discovery-symmetry contract (#162: all pipeline ops wait
  bounded; `depth` deliberately fail-fast as the discovery probe). Added as a key-facts
  bullet with its gate.

## §3 Orphans & dead links
Clean (wiki + front-doors + CLAUDE.md).

## §4 Coverage
Clean — all three delta commits (Run 41, #162, its ingest) have wiki homes after the §2
fixes; Run 41 itself lives in `docs/analysis/ratings.md` by design.

Pages touched: `CLAUDE.md`, `dev/testing/cluster-suites.md`, `dev/companions/tuple-space.md`.
