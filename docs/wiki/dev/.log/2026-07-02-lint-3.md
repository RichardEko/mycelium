## [2026-07-02] lint | third pass — catch the corrected KV-scope claim

Run after the classified-page examples exposed a mechanism error (§4.3.1's original claim
that a capability sub-group isolates classified bytes — false; KV floods the cluster).

**Check 1 (doc-vs-code):** clean. Lock table still 18 rows, field+alias grep reconciles
(no code changed since lint-2). Verified the *corrected* claim against source:
`WireMessage::Data` forwards `ForwardHint::All` (×6 in connection.rs) — KV is cluster-wide;
`Boundary::admits` gates Signal acting only. The wiki's runtime-invariants page already
stated "forwarding stays unconditional / only admission is scoped" — consistent, not
contradicted.

**Check 2 (staleness):** clean — all commits since lint-2 are docs (the mycelium-wiki
sketch + design record) or the toolchain pin, each with a matching dev/.log ingest.

**Check 3 (orphans/links):** clean — companions.md + the new runtime-invariants cross-link
resolve.

**Check 4 (coverage) — 1 finding, fixed:** the load-bearing substrate fact "KV floods the
cluster; a capability group is NOT a data-isolation boundary; the isolation boundary is the
cluster/mesh or encryption" lived only in the design record + a .log, not in a wiki page —
exactly the fact a contributor gets wrong (it *was* got wrong). Added it as a
runtime-invariants section ("KV floods the cluster — a group is not a data-isolation
boundary") with the served-path-gate vs cluster-boundary distinction and the code citation.
