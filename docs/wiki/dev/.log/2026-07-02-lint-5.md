## [2026-07-02] lint | fifth pass — clean (post Phase-1 diagnostics)

Run after the Legible-Emergence Phase-1 work + the dev/diagnostics.md catch-up. **No findings.**

**Check 1 (doc-vs-code):** clean. Lock-order table still 18 rows / 19 field decls (the 4 new
TaskCtx fields this session are `Arc<AtomicU64>`, not locks — table unaffected); every named test
gate cited in a wiki page exists (incl. the new
`test_p1_governed_group_conflict_detector_fires_and_clears_end_to_end`); every code-cited claim in
the new `diagnostics.md` verified against source — all 5 `/stats` gauges (`http.rs`), all 8
`mycelium_emergent_*` `/metrics` gauges (`emergent.rs`), the `emergent_detectors_enabled` /
`GOSSIP_EMERGENT_DETECTORS` flag, and the cited functions (`confirm_by_key`, `FlapTracker`,
`membership_snapshot`, `opacity_pairs`, `set_transitions`).

**Check 2 (staleness):** clean — the history.md "Legible Emergence proposed, not started" staleness
was the finding of the *previous* turn, now fixed; companions.md's mycelium-wiki "proposed/design-
complete" is still accurate (not built).

**Check 3 (orphans/links):** clean — all relative links resolve, every page reachable, every folder
has its folder-note.

**Check 4 (coverage):** clean — the diagnostics-layer coverage gap was closed by dev/diagnostics.md
(previous turn); the mycelium-wiki design-record content correctly lives in docs/design + docs/plans
(the wiki cites it via companions.md), not duplicated into the wiki.
