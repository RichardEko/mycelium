# 2026-07-24 — wiki-lint (post-SOC-2-arc)

Diff-gated over the 17-commit SOC 2 audit-gap arc merged since the 2026-07-21 lint (gateway TLS,
audit sink/checkpoint, identity auth 1a/1b/2/3, revocation glue, crypto-shred erasure).

## §1 Doc-vs-code
- **Lock-order table: ONE MISS (fixed).** `SubjectKeyRegistry::keys` (`mycelium-core/src/erasure.rs`,
  the WS-F crypto-shred `std::sync::Mutex<HashMap>`) was a new lock field absent from the table —
  the recurring lock-table-drift family, caught by the whole-workspace grep. Added as **row 31**
  (self-contained leaf, sync methods, no nesting/await). No other new lock (WS-C used OnceLock/mpsc;
  WS-E used papaya + atomics).
- **Watch-RMW sweep: CLEAN** (no `borrow()`+`send()` in the delta).
- **KV-namespace table: CLEAN** — all five new prefixes (`sys/identity-proof/`, `sys/audit/`,
  `sys/audit-checkpoint/`, `sys/revocation/`, `sys/capauthz/`) have `src/lib.rs` rows; erasure writes
  no KV prefix.
- **Front-door reserved list: CLEAN** — reserved set, companion claims, WIRE_VERSION (12), 8
  sub-handles all match code.
- **Endpoints/features (operations.md): 4 GAPS (fixed).** The new gateway surface was entirely
  absent: `POST /gateway/identity/revoke` + `identity:write` scope (WS-B), and the config fields
  `gateway_tls` (WS-A) and `require_identity_proofs` (WS-E Phase 3). Added an identity/TLS block to
  operations.md.
- **CI-gate list (testing.md): ONE GAP (fixed).** `make check` gained a `--features compliance`
  clippy this arc (the fix that closed the compliance-un-linted-locally gap), but testing.md's
  gate block didn't list it. Added. (The three 2026-07-21 gates remain correctly reflected.)
- **Named regression gates: CLEAN** (all cited test names resolve).

## §2 Staleness — 2 fixes
- `design/identity-authentication.md` said "Design … Not yet implemented" though all phases
  shipped → status flipped to **SHIPPED** with the two implementation deviations noted.
- `design/data-lifecycle-and-erasure.md` already flipped to SHIPPED during WS-F.

## §3 Orphans & dead links — CLEAN
All 35 wiki + guide-front-door relative links resolve (python sweep — the bash `realpath -m`
version false-positived on macOS; noted so a future pass doesn't trust it). Inbound
`examples/README.md#anchor` links clean; every folder-note present.

## §4 Coverage — 2 fixes
- **`design/data-lifecycle-and-erasure.md` (WS-F) was not cited from any wiki page** — the security
  page's subject embodies it. Added a WS-F pointer under security.md WS3 (composes with
  `DataAtRestCipher`).
- **The SOC 2 arc had no delivery-ledger entry** — added a "Post-v2.2.0" section to
  [history.md](../history.md) covering WS-A…F + the process fixes.

## No calibration entry
All findings are current-arc drift the lint correctly caught on its first pass over new work — no
area a *prior* lint declared clean regressed. (The lock-order/endpoint gaps are new code, not prior
misses.) Per the rule, no manufactured ledger line.

## Pages touched
`concurrency/lock-order.md` (+row 31) · `operations.md` (identity/TLS block) · `security.md` (WS-F
pointer) · `testing/testing.md` (compliance clippy) · `history.md` (post-v2.2.0 ledger entry) ·
`design/identity-authentication.md` (status) · this entry.
