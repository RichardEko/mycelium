# 2026-07-04 — lint pass (post access-broker)

Scope: the one code change since [lint-4](2026-07-03-lint-4.md) that the wiki cites is `4408331`
(the mycelium-wiki access broker). **Clean.** (The interim docs housekeeping — plan/README/ROADMAP/
CHANGELOG/guide/presentation — is outside `docs/wiki/`.)

**1 · doc-vs-code** — clean.
- Access broker: `dev/companions/wiki.md` cites `Wiki::request_store_access`, `Membership`, `StoreGrant`,
  `AccessError`, and `tests/access.rs` — all resolve in `mycelium-wiki/{src,tests}/`.
- Wire codec: `dev/history.md`'s "M11 codec (bincode retired, RUSTSEC-2025-0141)" is **accurate** —
  verified this session: the runtime/wire path uses the hand-rolled `mycelium-core/src/codec.rs` +
  `serde_fixint.rs`; `bincode 2.0.1` survives only as a **dev-dependency test oracle** (not in the
  non-dev tree — `cargo tree -e no-dev -i bincode` is empty). No "succession pending" claim anywhere.
- Lock-order table: **no** new `Mutex`/`RwLock` in `src/` or `mycelium-core/src/` since lint-4 (the
  broker's state lives in `mycelium-wiki/`, out of the table's scope) — table unaffected.

**2 · staleness** — clean. The access broker was ingested with its own commit (companion-page section
+ gates line). No `docs/wiki/` page describes a shipped item as pending.

**3 · orphans & dead links** — clean. `companions/wiki.md`'s relative links resolve.

**4 · coverage** — the access broker has a wiki home (companion page). Nothing uncovered.

Pages touched this pass: none (verification-only). No code bug surfaced. *(In-flight, separate from this
lint: retiring the bincode dev-only test oracle to golden vectors — clears the residual RUSTSEC-2025-0141
`cargo audit` warning; will update `dev/history.md` if the "retired" wording needs sharpening once done.)*
