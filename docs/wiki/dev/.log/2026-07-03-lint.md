## [2026-07-03] lint | dev/ — post-Legible-Emergence reconciliation (Phases 2–5 shipped)

First lint since 2026-07-02; between them all of Legible Emergence Phases 2–5 landed
(commits 17e2418…4991d6f, + the f54a5d2 opacity-flake hardening). Six findings, all doc-vs-code
staleness from the wiki not being fully reconciled to the shipped phases. Fixed directly.

### Check 1 — doc-vs-code verification
- **Finding (fixed):** `concurrency/lock-order.md` row 19 (`EventRing::events`) cited a `len()`
  method that was removed during Phase 3 (clippy dead-code — only `record()`/`since()` remain).
  Corrected the method list. The rest of the lock table verified complete: all 21 `Mutex`/`RwLock`
  field sites + the 2 lock-wrapping aliases (`GossipRxs`, `SenderLog`) map to the 19 rows — no
  undeclared locks (the papaya `commit_conflict_slots` map is lock-free, correctly not a row).
- Named regression gates: all test names cited across the wiki exist
  (`narrate_*`, `test_explain_fanout_*`, `test_fleet_diagnosis_*`, `test_fleet_snapshot_*`,
  `test_gateway_fleet_snapshot_endpoint_scope_gated`, `test_p1_*`, …). Clean.
- Constants/flags: `emergent_detectors_enabled` (default false), `GOSSIP_EMERGENT_DETECTORS`,
  `CONFIRM_TICKS=2`, and the `mycelium_emergent_*` gauges all verified against `src/agent/emergent.rs`
  + `mycelium-core/src/config.rs`. Endpoints `/gateway/{fleet,explain,diagnose}` all present in
  `src/agent/http.rs`. Clean.

### Check 2 — staleness (the bulk of the findings)
- **`history.md` (fixed, major):** said "Phases 2–5 … are **not started**" — replaced with the
  per-phase shipped ledger (0–5 complete) + the three-verb spine summary.
- **`operations.md` (fixed):** "Diagnostics surface" listed only `/gateway/fleet` — added the full
  localize/explain/diagnose spine, the RT3 non-responder + RT1/RT2 caveat notes, the programmatic
  `fleet_snapshot()`/`fleet_diagnosis()` API, and a link to the new `operations/diagnostics.md` runbook.
- **`dev.md` folder note (fixed):** diagnostics.md described as "Phase 1" only → "Phases 1–5,
  complete" + the three verbs.
- **`diagnostics.md` (fixed):** the "Status & what's next" section had accreted contradictory labels
  ("Phase 2 in progress", "Phase 3 in progress", "Not started: Phase 5") beside newer complete
  sections. Reconciled to "complete, all phases 0–5", and added the Phase-5 operator-surface
  paragraph (public API, two-audience docs, the coop `diagnostics` demo).

### Check 3 — orphans & dead links
Clean. All relative links in the touched pages resolve (incl. the cross-tree
`../../operations/diagnostics.md`); `diagnostics.md` reachable via `wiki.md → dev.md`.

### Check 4 — coverage
`testing.md` already carries the durable session lesson (the "feature-gated dead code is a real
trap" note + the `--no-default-features` and `-p mycelium-wasm-host` clippy gates, added during the
work). The two recurring CI timing-flake hardenings (opacity-gate 10→30 s, partial-meshes 8→20)
live in the calibration ledger (`docs/analysis/ratings.md`) — the correct home, not the wiki. No
uncovered durable knowledge.

Pages touched: `concurrency/lock-order.md`, `history.md`, `operations.md`, `dev.md`,
`diagnostics.md`. No code bugs surfaced (the `len()` drift was a stale citation, not a wrong claim).
No user decision needed.
