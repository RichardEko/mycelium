# 2026-07-15 — wiki-lint (6th pass; post capability-matrix + run-docs)

Diff since `lint-5`: the examples **capability matrix** (single faceted front-door table + the evolved
`examples-layer-matrix.html`), five **new example-directory READMEs** (blackboard · tuple-space · wiki ·
reason · guardrails), and the matrix-link repoint (source → run-docs). **Docs-only diff** (`git diff
--stat` shows no `src/`/`mycelium-*/src/` change), so §1's code-verification sub-checks (lock table, KV
namespace, cited constants, CI gates) were not triggered — nothing they cover moved.

## §1 Doc-vs-code — the new run-command claims
Verified each new README's run command against its crate's `Cargo.toml` `required-features`:
`wiki_council_viz` (req `gateway,llm` — README adds `metrics`, correct) · `reason_node` (`llm,gateway`) ·
`guardrail_viz` (`compliance,gateway` + `metrics-export`) · `redistribution_viz` (no req-features; README
passes `gateway,metrics` explicitly). All consistent — the READMEs add `metrics`/`metrics-export` on top
of the required set (wanted for the Ops Console Metrics tab, not required to build). CI-marker claims
(`WEDGE OK`/`FLEET OK`) trace to real strings in the guardrail `.rs` + `mycelium-guardrails/ci_smoke.sh`.
No findings.

## §3 Orphans & dead links — **1 finding, fixed** (+ structural sharpening)
- **DEAD ANCHOR (fixed):** `docs/guide/09-security.md:279` linked the Ops Console Audit tab via
  `examples/README.md#the-suites` — the matrix restructure renamed that section to *The worlds*, so the
  anchor died. Repointed → `#ops-console` (the correct current home). This was the **second** inbound-anchor
  break from the same restructure (`#find-one-by-layer` → `ui-example-contract.md` was fixed inline during
  the matrix commit), so it earned a calibration entry + a **§3 sharpening**: the dead-link check now sweeps
  **inbound** `examples/README.md#anchor` links (`grep -rnoE`), not just outbound front-door links — a
  heading rename breaks inbound anchors silently. Folded into the skill.
- All 20 capability-matrix link targets resolve (guide chapters 01/02/04/05/11 · the five new dir READMEs ·
  coop/chat/fluid/a2a/langgraph/community · same-page `#research-artifacts`/`#ops-console` · root `#demos`).
- All outbound relative links in the five new READMEs resolve (guide ch.15, langgraph README,
  ui-example-contract, shared-setup/ops-console anchors).

## §4 Coverage — 1 improvement
The five new READMEs are durable run-docs whose subject `examples.md` owns but did not cite (it pointed at
dirs/`.rs`). Updated `examples.md`'s Index paragraph: **every example directory now carries a README**
following the doc template (companion/reason/guardrails added 2026-07-15), so the matrix links land on
run-docs. Category enumeration remains complete (the 2026-07-15 ledger fix's guardrails/reasoning/wiki
categories are present); the page cites `examples/README.md § The capability matrix` as source of truth.

## §2 Staleness — clean
No shipped-vs-planned drift in the diff window.

## Pages touched
`docs/guide/09-security.md` (dead anchor), `docs/wiki/dev/examples.md` (coverage note),
`docs/wiki/dev/.log/lint-calibration.md` (+ inbound-anchor entry), `.claude/commands/wiki-lint.md`
(§3 inbound-anchor sweep folded in).
