# 2026-07-10 — lint 5 (post-Run-43 — clean pass)

Delta since lint 13 (81a70eb): the Run 43 analysis commit only — **test-only** (three new
falsification probes in `src/lib_tests.rs`) + `ratings.md`. No wiki page, guide, front-door,
or `.md` link changed.

## §1 Doc-vs-code — clean
- Lock/constant/KV-prefix sweeps trivially clean (test-only code diff; zero new lock sites).
- The three new gates exist (`probe_gateway_auth_gates_the_wire`,
  `probe_config_rejection_names_the_field`, `probe_tombstone_survives_replay_of_older_write`).
- `dev/security.md`'s auth model (WS1 four layers + WS4 OIDC) matches `rbac.rs`/`http.rs`/
  `oidc.rs` and is *confirmed* by the new wire-auth probe — no drift, no new invariant.
- Run 43's ratings entry cites no dead paths (the #163 `kv.rs`→`topology/introspect` rename
  is already reflected; entry references `topology.rs`/`introspect.rs` correctly).

## §2/§3 — clean (no pages touched; anchor + link state unchanged since lint 13).

## §4 Coverage — one judgment recorded
The three new probes are **M2 falsification probes** — by series convention they live in
`ratings.md` (the analysis run owns them), not the wiki's named-gate lists. They prove
existing documented invariants (gateway-auth-gates-the-wire, config-rejection-names-the-field,
tombstone-anti-resurrection) rather than establishing new ones, so **no wiki page is owed**.
The one genuinely-new artifact — an *end-to-end* wire auth test where only unit-level scope
resolution existed — is a test-coverage improvement, not durable architectural knowledge.

Pages touched: none (clean pass).
