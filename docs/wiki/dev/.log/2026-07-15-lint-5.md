# 2026-07-15 — wiki-lint (5th pass; post UI-example contract)

**Clean pass — the new check earned its keep on first run.** Delta since `2026-07-15-lint-4`: the
**UI-example contract** (`dev/ui-example-contract.md`, new) + the concepts box on all nine browser
examples + the folded lint check (§4). No product-code change (`git diff --stat … src/
mycelium-*/src/` empty — examples, wiki, and the skill only).

## §3 Orphans & links
Clean: the new `ui-example-contract.md` is reachable — linked from `dev.md` (the folder-note) and
`dev/examples.md`. No orphan.

## §4 Coverage — the new UI-example-contract check, first exercise
Applied the folded check (every browser example: `ui/viz` + `__OPS_CONSOLE_LINK__` + `__CONCEPTS__`
inject, run command carrying `gateway,metrics`) to all nine browser examples: **all pass** —
`conway` · `microgrid_viz` · `stigmergy_viz` · `llm_council_viz` · `redistribution_viz` ·
`wiki_council_viz` · `guardrail_viz` · `llm_agent` via the injected mechanism, `three_node_demo` via
the documented static-panel exception (`conway-gpu` is the no-gateway exception). Notably the check
would have caught `llm_agent`'s previously-missing Ops Console link — which the contract work fixed —
so it is a real check, not a rubber stamp. No findings.
