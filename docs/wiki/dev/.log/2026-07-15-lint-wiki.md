# wiki-lint — 2026-07-15 (post audit Runs 50–58)

Run after the five-pass consensus/concurrency audit + follow-ups (~40 code fixes, the identity-auth
design, the `/ready` and reorder-buffer feature-fixes). Clean-ish — most wiki-relevant changes were
already ingested *during* the session (security.md `sys/identity` gap + design-doc link).

## §1 Doc-vs-code
- **Lock-order table:** no change — the session added atomics (`KvStore.live_count`, per-param timing
  pins) but **no new `Mutex`/`RwLock`**; verified by diffing the merge range.
- **Named gates:** the `/ready` test was renamed (`ready_gate_flips_on_first_capability_advertisement`
  → `ready_reflects_startup_not_advertisement`) — **not cited anywhere in the wiki**, no break.
- **CI-gate block:** accurate; the nightly cargo-fuzz job is mentioned generically (non-PR) and does
  not enumerate targets, so the new `frame_apply` target needs no wiki edit.
- **Cited constants:** wire `v12`/PREV `11` unchanged. No cited constant drifted.

## §2 Staleness — 1 FINDING (fixed)
- `dev/operations.md` `/ready` row read **"soft-state advertised + no dead shards"** — stale: this
  session changed `/ready` to reflect **startup completion** (capability discovery no longer gates
  readiness). Fixed the row. (Not a calibration-ledger entry: the row was correct until this session's
  code change; same-session drift caught promptly, not a prior-lint miss.)

## §3 Orphans / dead links
- The `docs/design/identity-authentication.md` link added to `security.md` this session resolves;
  the design doc is covered (linked from the security page). No new page, no folder-note needed.

## §4 Coverage — 1 addition
- The audit series' most-repeated bug family (peer/config-supplied numeric → overflow/panic/wrap) and
  its new structural gate (a no-panic-on-untrusted-input **proptest suite under overflow-checks**, +
  the nightly `frame_apply` decode→process target) had **no wiki home**. Added a concise
  "Input-fuzz gate" note to `dev/testing/testing.md` stating the invariant (arithmetic on untrusted
  values must saturate/clamp), citing the six proptest gates (all verified to exist), and noting the
  gate is not yet comprehensive (the Robustness dimension in `ratings.md` stays floored until a pass
  validates the sweep).

No calibration-ledger entry: this pass surfaced no *prior-lint* miss (the one drift was same-session).
