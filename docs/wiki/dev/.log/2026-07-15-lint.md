# 2026-07-15 — wiki-lint (post examples completeness-sweep + Audit tab)

**One finding — a calibration hit, fixed.** Scoped to the delta since `2026-07-14-lint-4`
(baseline `bcd235a`): the README single-flow restructure, the layer-map matrix, the guardrails +
companion-crate example sweep (36→42), and the Ops Console Audit tab. No product-code change
(`git diff --stat bcd235a..HEAD -- src/ mycelium-*/src/` empty), so §1's core checks were unaffected.

## §1 Doc-vs-code · §2 Staleness · §3 Links
Clean. No substrate change → lock/wire/KV untouched. The new category descriptions in `examples.md`
(guardrails/reason/wiki) are from the examples' own doc-comments — no pinned constants. The one new
cross-doc link (`examples.md → ../../guide/15-reasoning-and-langgraph.md`) resolves; the three new
crate refs are backticked, not links.

## §4 Coverage — the finding (calibration hit on the by-category check itself)
`dev/examples.md` enumerated 7 categories (starter · coop · visual-showcases · ops-console · AFN ·
a2a · integration) but **three whole categories were absent** — **Guardrails**
(`mycelium-guardrails/examples/`), **Reasoning / LangGraph** (`mycelium-reason/examples/` + the Rust
reason nodes the FAQ now cites), and the **Wiki companion** (`mycelium-wiki/examples/wiki_chat.rs`) —
plus **Skills / community** (the audit demo) missing as its own category. **Fixed:** added four
category bullets.

**Why this is a calibration hit, not just a finding:** these examples *pre-date this session*, so
lints 3–4 should have caught them — but the 2026-07-14 by-category sharpening pinned the category list
to a **hardcoded set** (`starter · coop · AFN · a2a · integration · visual-showcases`) that never
included guardrails/reasoning/wiki. A fixed enumeration is the same drift-bug as a pinned count, one
level up. Root scope error: the examples audit only ever swept `examples/` + coop, so every companion
crate's `examples/` was invisible. Found by a **user question** ("no examples for viewing audit or
guardrails?") → a workspace-wide sweep. **Ledger entry + structural sharpening recorded** (2026-07-15):
derive the category set from the tree (`find . -path '*/examples/*.rs'` across all crates), never a
fixed list; and `dev/examples.md` should cite `examples/README.md` § The suites (now workspace-complete)
as canonical so the wiki synthesizes instead of maintaining a parallel list. Folded into the skill's §4.

**Pages touched:** `docs/wiki/dev/examples.md`, `.log/lint-calibration.md`, `.claude/commands/wiki-lint.md`.
**User decisions:** none.
