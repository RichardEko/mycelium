# 2026-07-10 — lint 4 (delta pass after the two docs passes)

Docs-only delta (front-page restructure e118dd5/b9fc838 + operations pass 7aad48a) — lock/
gate/constant sweeps trivially clean (no code diff).

## New check introduced: anchor-fragment verification
File-existence link sweeps can't catch a `#fragment` that doesn't match any heading slug, and
the restructure created a dozen new `#reference--…` deep links. Added a GitHub-slugification
checker over all md links (docs/ + examples/ + README/CLAUDE/ROADMAP, cross-file and
same-file). Result: every restructure-era anchor resolves; **one pre-existing break found**
— `cookbook.md → README.md#which-crate` (short form never matched the real slug) — fixed to
the full slug. This checker is worth re-running in future lints after any heading churn.

## §1 Doc-vs-code — 1 finding, fixed
`dev/operations.md`'s exhaustive `/stats` row omitted `individual_flood_fallbacks` and the
liveness fields (`dead_shards`, `gc_alive`, `health_monitor_alive`) that `src/agent/http.rs`
serves — the same gap fixed in `docs/operations/observability.md` during the ops pass, now
fixed in the wiki's copy too (with the connect_peer remedy pointer).

## §2/§3/§4
Clean: faq's README references point at surviving anchors (`#run`, the full which-crate
slug); CLAUDE.md routing rows all valid; both docs passes have log entries; no orphans.

Pages touched: `dev/operations.md`, `docs/guide/cookbook.md`.
