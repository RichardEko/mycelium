# 2026-07-15 — wiki-lint (3rd pass; post wiki_council_viz local-LLM wiring)

**Clean pass — no findings.** Delta since `2026-07-15-lint-2`: `wiki_council_viz` now phrases each
specialist's grounded answer via a **local model served on the mesh** (Ollama registered as
`llm/{model}`, invoked over the mesh via `call_prompt_skill`), with a grounded-extraction fallback.
No product-code change (`git diff --stat 6ff383f..HEAD -- src/ mycelium-*/src/` empty — the change is
the example + Cargo registration + docs; the `mycelium-wiki` lib is untouched). §1: the example's
`required-features = ["gateway", "llm"]` and the run command in `dev/examples.md`
(`--features gateway,llm`) agree. The wiki-companion description accurately reflects the code (local
Ollama, `register`/`call_prompt_skill`, fallback) — no drift.
