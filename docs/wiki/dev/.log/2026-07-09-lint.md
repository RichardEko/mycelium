# 2026-07-09 — wiki lint (dev section)

Scope: work merged since the 2026-07-08 lint-2 (`4fa1af5`) — #140/#141 (test-util
`alloc_port`), #142/#143 (hello_mesh/hello_capability starters), #144 (operator docs), plus
the in-flight deploy branch (#145, k8s + Terraform). Run on branch `deploy/kubernetes`.

## §1 Doc-vs-code verification — 1 finding

- **Lock-order table** (`concurrency/lock-order.md`): **clean.** Full-workspace grep
  (`Mutex<`/`RwLock<` + type aliases) — every site maps to a row (1–30). The two new
  companions add **zero** lock sites (`mycelium-reason`, `mycelium-guardrails` hold no
  locks). Spot-verified the ones a keyword grep missed: `OidcVerifier::cache` → row 17,
  `gossip_rxs`/`GossipRxs` alias → row 5, `signal_boundary` (context.rs) → row 4.
- **KV-namespace table** (`src/lib.rs`): **clean.** All `mycelium-reason` prefixes present
  (rows 122–127). Verified two grep hits were not new prefixes: `models/{model}/blob`
  (reheal example) is a **blob-tier** key, not gossip KV; `reason/{run_id}` goes through
  `kv().append` which prefixes `log/` → real keys `log/reason/…` = row 125.
  `mycelium-guardrails` writes **no** KV prefix (policy/verify layer over the public API).
- **Constants:** `WIRE_VERSION = 12`, `PREV_WIRE_VERSION = 11` — matches
  `building-on-mycelium.md` ("currently 12").
- **Sub-handle accessors:** all eight match code (`kv/mesh/schemas/consensus/service/`
  `capabilities/mcp/llm`); front-door's `schemas()` is correct.
- **Named gates / test-util:** `testing/testing.md` already documents the Run-39 `test-util`
  feature + `alloc_port` (lines 75, 90–99) — no drift.
- **F1 (Minor) — FIXED:** `companions/companions.md` listed `/gateway/reason/{blob,trace}`
  but `mycelium-reason/src/http.rs` serves `{blob,trace,route}` (the `route` load-aware
  surface, #132). Added `route` + a one-clause gloss.

## §2 Staleness — 1 finding

- **F2 (Minor) — FIXED:** `dev/examples.md` (the runnable-suite catalogue) predated the
  #142/#143 starter ladder. Added a lead bullet: `hello_mesh` → `hello_capability` →
  `llm_agent`, the zero-LLM junior-dev entry point the README front-door now leads with.
- Deploy branch (#145): the durable knowledge (multi-host escape from the single-host
  Docker-bridge ceiling) is ingested in `testing/scale-tests.md` as part of the PR itself —
  no separate action.

## §3 Orphans & dead links — clean

Every wiki relative link resolves; every folder has its folder-note; the front-door guide
docs (`faq.md`, `building-on-mycelium.md`) have no dead links.

## §4 Coverage — clean

`test-util`, both new companions (`companions.md`), and the deploy multi-host story all have
homes. No new top-level section needed.

Pages touched: `dev/examples.md`, `dev/companions/companions.md`.
