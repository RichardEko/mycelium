# 2026-07-13 — wiki-lint (post wiki-store-CAS + coordination design note)

**Clean pass — no findings.** Scoped to the delta since the last lint: the only merges since
2026-07-11 are this session's four commits (`8456dc4`/`a3bda05` section-granular CAS for the wiki
store; `d316cdf`/`db08970` the `coordination-approaches.md` design note), all ingested in the same
session. Verified, did not need to fix:

## §0 Calibration ledger
Reviewed `lint-calibration.md` first. The two repeat-miss areas it flags (lock-order table
completeness; the reserved-prefix / KV-namespace list) were the primary targets below.

## §1 Doc-vs-code (load-bearing)
- **Lock-order table** — the CAS work added **no locks** (the write path is lock-free: `std::fs::hard_link`
  + `AtomicU64` tmp-seq, no `Mutex`/`RwLock`). Row 30 already covers the wiki's five `parking_lot::Mutex`
  fields (`last_lint, curator_reg, candidate_reg, tasks, curator_tasks`) — unchanged. Whole-workspace
  grep: 70 field sites + 2 lock-wrapping aliases (`GossipRxs`, `SenderLog`), all previously declared.
- **Named regression gates** — all seven test names cited in `companions/wiki.md` exist:
  `concurrent_curators_editing_different_sections_dont_clobber`, `same_section_stale_write_is_rejected_not_silently_lost`,
  `manifest_membership_is_compare_and_swap`, `a_stale_write_into_a_gc_gap_is_rejected_not_shadowed`,
  `concurrent_idempotent_appends_deliver_every_edit_exactly_once`, `many_threads_editing_different_sections_all_survive`,
  `dual_curators_reconcile_to_a_single_writer`.
- **Cited constants** — `WIRE_VERSION = 12`, `PREV_WIRE_VERSION = 11` in `framing.rs`; guide chapter
  `09-security.md` pins `wire v12` — consistent. (No wire change this session.)
- **New design-note code citations** (`coordination-approaches.md`) — all resolve: `read_versioned` /
  `write_section` / `update_manifest` (`mycelium-wiki/src/store.rs`), `elect_leader`
  (`src/agent/consensus_handle.rs`), `put_with_id` / `fetch_max` (`mycelium-tuple-space/src/`),
  `WikiError::Conflict` (`mycelium-wiki/src/model.rs`), `LockGuard::token` (`src/agent/overlay_consistent.rs`).
- **KV-namespace / reserved-prefix, CI-gate list, endpoint/feature lists** — untouched by this
  session (the CAS is filesystem-level, not KV; no new routes/features/CI steps), so no new drift.

## §2 Staleness
No stale pages. The one page that could have drifted — `companions/wiki.md`'s "single-writer dividend:
no lost update" — was updated in the same session to the CAS/at-least-once framing; the design record
`wiki-concurrent-edit.md` §3.5 (had accepted "eventual-single, not strict-single") was corrected too.
No other wiki page describes the wiki store write path (grep confirmed; `pattern-coverage.md:96` is an
analogy, not a behavior claim).

## §3 Orphans & dead links
The new ingest entry `2026-07-13-wiki-store-cas-airtight.md` follows the `# YYYY-MM-DD — <topic>`
header convention the peer entries use (the `log.md` README is a convention note, not an index — no
registration needed). Links in the touched pages resolve.

## §4 Coverage
This session's durable knowledge already has homes: `companions/wiki.md` (the CAS behavior + the
at-least-once nuance a stress test surfaced), this section's ingest entry, and the user-facing
`docs/design/coordination-approaches.md` (cross-linked from `04-consensus.md`, `companions.md`,
`exactly-once-effect.md`, `wiki-concurrent-edit.md`, the guide FAQ, and `docs/README.md`).

No calibration-ledger entry: this pass surfaced no drift a prior lint had declared clean.
