# 2026-07-08 — wiki-lint 2 (post v3.0 completion: #132–#139)

Second lint today, after the full mycelium-reason ladder (#132–#136) and the entire
mycelium-guardrails track (#137–#139) merged. The prior lint (2026-07-08-lint.md) ran after only the
reason crate + Python tier (#130/#131), so the ledger and positioning had stopped at #131 and still
called guardrails "PROPOSED." Almost entirely a **staleness + coverage** pass; §1 doc-vs-code was clean.

## §1 doc-vs-code — clean

- **Locks:** zero `Mutex`/`RwLock` in either new crate (`mycelium-reason`, `mycelium-guardrails`) —
  lock-order table stays 30 rows, no drift.
- **KV prefixes:** the only new prefix from the merged work is `reason/` (blob-cache cap + `log/reason/`),
  already added to the `src/lib.rs` table in lint-1. Guardrails composes existing prefixes
  (`grp/`·`agent/`·`cap/`·`sys/audit/`) — no new rows.
- **Cited tests exist:** `boundary_transition_signals_are_never_locally_shed`,
  `dual_curators_reconcile_to_a_single_writer`, `liveness_filter_drops_a_non_peer_cap` (the new router
  canary) all present. **WIRE_VERSION** still 12.

## §2 staleness — the main work, 4 pages fixed

- **`dev/history.md` (delivery ledger):** the "v3.0 positioning — PROPOSED, not started" bullet and the
  mycelium-reason entry (which said "first two workstreams… guardrails stays PROPOSED", stopping at #131)
  were stale. Fixed the positioning bullet to record it *preceded* the shipped work; rewrote the reason
  entry to **COMPLETE (#130–#136)** with the ladder/flagship/router-fix/chapter-15/Ollama detail; **added
  a full `mycelium-guardrails` entry (#137–#139)** — the three strength tiers, the Tier-C gate + denial
  sealing, the `prove_denials` verification tool + honest framing, the examples, self-imposed stance.
- **`domain/pattern-coverage.md`:** the Structural-guardrails section read as *proposed* ("Now a primary
  v3.0 deliverable") → rewrote to **✅ SHIPPED (#137–#139)** with the three-tier framing. The LLM-DX
  section marked only #130/#131 → added the full ladder (#132–#136).
- **`ROADMAP.md`** (v3.0 home, cited by the wiki): both primaries still "proposed / still proposed" →
  both marked ✅ shipped; the guardrails paragraph rewritten around the three tiers + `prove_denials` +
  the self-imposed stance.

## §4 coverage — 1 gap filled

- **`dev/companions/companions.md`** listed `mycelium-reason` (from lint-1) but not `mycelium-guardrails`
  — added the entry; and refreshed the reason line from "#130/#131" to COMPLETE (#130–#136).

## §3 orphans / dead links — clean

All relative links in the edited pages + guide chapter 16 resolve. Chapters 15/16 are indexed in the
guide README. Nothing needing a user decision (no new section).

**Pages touched:** `dev/history.md`, `dev/companions/companions.md`, `ROADMAP.md`, and (domain)
`domain/pattern-coverage.md` — logged separately under `domain/.log/`.
