# 2026-07-15 — wiki-lint (7th pass; post matrix-offpage-links + ops_console externalization)

Diff since `lint-6`: matrix links repointed off-page (llm_agent → guide ch02, the two research runners →
their design/plans docs), **`ops_console` moved into its own directory** (`examples/ops_console/{main.rs,
ops_console.html,README.md}` + a pinned `[[example]]` path), and **`The worlds` slimmed** to the
`## Browser showcases` run table. **Docs + examples-config only** — no `src/`/crate-`src` change (the two
touched `.rs` are comment-only), so §1's code-verification sub-checks were not triggered.

## §3 Orphans & dead links — clean
- The `ops_console` move: all inbound refs to the old `examples/ops_console.rs`/`.html` were repointed in
  the move commit (examples.md → `examples/ops_console/`, two code comments → `ops_console/main.rs`); the
  only remaining `ops_console.html` mentions are the *relative* `include_str!` + the new README's source
  link, both correct. Historical `docs/analysis/{ratings,doc-coverage}.md` mentions left intact (dated
  measurements — policy).
- Inbound `#anchor` sweep (the check added in lint-6): the `The worlds → Browser showcases` rename broke
  two `#the-worlds` anchors — **already fixed in the slim commit** (matrix note + `ui-example-contract.md`
  → `#browser-showcases`); re-verified all four inbound `examples/README.md#…` anchors resolve
  (`#the-capability-matrix` · `#browser-showcases` · `#ops-console` · `#shared-setup`). The `#ops-console`
  heading was deliberately kept as a pointer so its four inbound links (09-security, ch02, reason/wiki
  READMEs) still resolve.

## §4 Coverage — **1 finding, fixed** (+ structural sharpening)
- **UNCLASSIFIED BROWSER EXAMPLE (fixed):** the UI-example-contract check enumerates browser examples as
  `grep -rl 'include_str!.*\.html' examples` and had been verifying the 9 showcases + naming 2 exceptions
  — but never reconciled against the *full* grep, so **`ops_console`** (which also `include_str!`s an
  `.html`) sat unclassified every pass. It is legitimately an **exception**: the console *itself* — the
  consumer/observer of the `ui/viz` convention, not a showcase that advertises it — so rules 2 (advertise
  + inject back-link) and 4 (concepts box) don't apply. Added it to the exceptions in
  `ui-example-contract.md`; verified all 10 `include_str!` hits now classify (8 showcases + `llm_agent`
  compliant · `conway-gpu` + `ops_console` exceptions; `three_node_demo` is the inline-HTML exception,
  not in the grep). **Sharpening:** the check now requires classifying *every* hit as compliant-or-
  exception — an unclassified hit is itself a finding (folded into §4 + the contract's Lint section +
  calibration ledger).
- Minor: the skill's §4 durable-fix note cited `examples/README.md § The suites` — a section that no
  longer exists (renamed to *The capability matrix*). Repointed.
- `examples.md` remains accurate: cites `§ The capability matrix` as source of truth; the ops_console
  bullet now reads `examples/ops_console/`; the `llm_agent (Mesh Control …)` mention is a live demo
  reference, not a stale section.

## §2 Staleness / §1 — clean/not-triggered
No shipped-vs-planned drift; no code-cited claim moved.

## Pages touched
`docs/wiki/dev/ui-example-contract.md` (ops_console exception + Lint reconcile rule),
`.claude/commands/wiki-lint.md` (§4 classify-every-hit sharpening + stale §-ref fix),
`docs/wiki/dev/.log/lint-calibration.md` (+ the unclassified-hit entry).
