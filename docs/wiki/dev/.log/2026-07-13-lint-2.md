# 2026-07-13 — wiki-lint (2nd pass today; post gateway-overhead bench)

**One finding (§4 coverage), fixed.** Scoped to the delta since the earlier 2026-07-13 lint. Nothing
in `docs/wiki/` changed since; the delta is **code + non-wiki docs**: the new `benches/gateway_overhead.rs`
(closing the persuasion-surface `~1 ms` item), the `coordination-approaches.md` consistency section,
and the CAP deck slide.

## §1 Doc-vs-code
Clean for the delta:
- **No wiki page enumerates the benchmarks** (grepped `benches/` / `throughput` / `signal_fanout` /
  `criterion` across `docs/wiki/`), so the new `gateway_overhead` bench creates no doc-vs-code gap.
- **The bench added no locks** (`grep Mutex/RwLock benches/` empty) — lock-order table unaffected.
- `companions/blackboard.md` "at-least-once" and `companions/wiki.md` (updated in the earlier pass for
  the section-CAS) remain accurate.

## §4 Coverage — the finding
The new **`docs/design/coordination-approaches.md`** (the cross-cutting "why ring-elected single-writer,
not the CP lock" decision + CAP framing that all three companions embody) was referenced from the
guide, operations, and other design docs — but **not from the wiki**, only from the `.log`. The wiki's
own contract is *"code is canon, the wiki cites it"*; a decision the companions all embody should be
citable from the companions synthesis. *Fixed:* added a pointer in the `companions/companions.md`
folder-note, at the spot that already describes the shared "capability-ring failover" shape.

## §2 / §3
No staleness (the only merges since the last pass are code/non-wiki docs, and the coordination material
was ingested same-session). Links in the touched page resolve.

## Calibration
Logged a sharpening (see `lint-calibration.md`, 2026-07-13): §4 should verify the wiki **cites** a new
authoritative design doc, not merely that the doc exists — the earlier pass this day treated
"coordination-approaches.md exists (user-facing)" as sufficient coverage without checking the wiki
referenced it. Folded into the skill's §4.
