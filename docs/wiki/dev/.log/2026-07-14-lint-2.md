# 2026-07-14 — wiki-lint (2nd pass today; post Ops Console)

**Clean pass — no findings.** Scoped to the delta since `2026-07-14-lint`: the **Ops Console**
(`examples/ops_console.rs` + `.html`, `:8099`) and the gateway wiring on the continuous examples
(conway viewer `:9090`; `stigmergy_viz`/`llm_council_viz` print their depot gateway URL;
`microgrid_viz` `:9091` / `redistribution_viz` `:9093` under `--features gateway`), plus the
analysis/doc-coverage/publication-lint run entries and the scale-runner/conway fixes.

## §1 Doc-vs-code
Clean:
- **Gateway ports verified against code** — `OPS_PORT` consts (9090 conway, 9091 microgrid_viz, 9093
  redistribution_viz) and `CONSOLE_PORT` 8099 exactly match the ports the wiki `dev/examples.md` Ops
  Console + Visual-showcase bullets state.
- **Lock-order table unaffected** — product-lock count still 70; the Ops Console (`Arc<reqwest::Client>`,
  no `Mutex`/`RwLock`) and the `http_port` field additions are all in `examples/`, out of the table's
  `src/`·`mycelium-*/src/` scope. No product-code change this delta.
- No new named regression gates cited; wire constants untouched.

## §4 Coverage — handled proactively (no finding)
The Ops Console is a **new example** and is already cited from both enumeration surfaces — the wiki
`dev/examples.md` (its own bullet) and the front-door `examples/README.md` — added in the same commit
that built it (`79d9520`). The category-completeness check (ledger 2026-07-14) passes: `examples.md`
covers starter · coop · visual-showcases · **ops-console** · AFN · a2a · integration. This is the
"wiki cites it" / "audit by category" discipline applied at authoring time rather than caught later —
which is the point of the sharpenings.

## §2 / §3
No staleness (delta freshly ingested). The new bullets use inline-code paths (`examples/ops_console.rs`),
which resolve; no markdown links to break.

## Calibration
No entry — nothing a prior lint declared clean was found drifted; the new coverage was added
proactively, not missed.
