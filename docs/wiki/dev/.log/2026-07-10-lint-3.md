# 2026-07-10 — lint 3 (delta pass after Run 42 + #163 refactor)

## §1 Doc-vs-code
- **Clean, with two triage decisions:**
  - `docs/plans/legible-emergence.md` + `docs/plans/v2-m3-core-handles.md` still cite
    `src/agent/kv.rs` (deleted in #163). **Deliberately left**: plans are dated design
    records, historically accurate at their writing — same non-rewrite convention as
    `ratings.md` entries. The live wiki citations were already fixed post-merge
    (`aaa8498`: diagnostics.md → `introspect.rs`, runtime-invariants.md → `topology.rs`).
  - The `+ signal_boundary: Arc<parking_lot::RwLock<…>>` line in the #163 diff is a
    **reference field** in `GcContext` to the existing row-4 lock — not a new lock site;
    no lock-order row needed.
- All three Run-42 probe gates exist; `WIRE_VERSION 12`/`PREV 11`; no new KV prefix
  writers; lock aliases unchanged (SenderLog, GossipRxs, test-scope ProgressLog).

## §2 Staleness
Clean — #163 is in CHANGELOG; the two live wiki citations were fixed same-day.

## §3 Orphans & dead links
Clean (wiki + front-doors + CLAUDE.md).

## §4 Coverage
One judgment recorded: the **spawn-context-struct idiom** (#163 — "spawn tasks take a
Context struct, ListenerContext-style; add fields, not positional params; same-typed
adjacent positional params are a silent-swap hazard") is carried by the in-code doc
comment on `GossipShardContext` and the CHANGELOG entry. **Deliberately not filed as a
wiki page** — the wiki has no code-conventions section and one bullet doesn't justify one;
revisit if the idiom question recurs.

Pages touched: none (this pass found no doc edits needed — the post-merge citation fix
had already landed).
