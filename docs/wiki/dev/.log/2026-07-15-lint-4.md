# 2026-07-15 — wiki-lint (4th pass; post guardrail_viz + showcase metrics)

**Clean pass — no findings.** Delta since `2026-07-15-lint-3`: the `guardrail_viz` browser showcase
(`mycelium-guardrails/examples/`, `:8096`) + a `gateway` feature on that crate, and the
showcase-metrics change (a `metrics` feature on blackboard/tuple-space/wiki/coop, `metrics-export` on
guardrails, and every showcase run command updated to carry it). No product-code change
(`git diff --stat 0ad4b3d..HEAD -- src/ mycelium-*/src/` empty).

## §1 Doc-vs-code
Clean: the updated run commands match the crate features they cite — `metrics-export` exists in
guardrails (the name avoids its `metrics` *dependency* collision), `metrics = ["mycelium/metrics"]`
exists in the four other showcase crates; the README/wiki run commands and each example's doc-comment
all carry the right `--features`.

## §4 Coverage — proactive, no finding
`guardrail_viz` is present in all four enumeration surfaces — README (Browser showcases + by-layer
security/policy), `dev/examples.md` (Guardrails category), the layer matrix (44), and the deck. The
Guardrails category (added lint 2026-07-15) now lists all three: `guardrail_wedge` · `guardrail_fleet`
· `guardrail_viz`. Indexed at authoring time — the by-category discipline working before the lint, not
caught by it.
