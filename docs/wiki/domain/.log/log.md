# domain/.log — ingest history

One dated file per ingest: `YYYY-MM-DD-<slug>.md`, containing
`## [YYYY-MM-DD] ingest | <topic>` plus a few lines. Never append. Lint passes logged as
`YYYY-MM-DD-lint.md`.
